# Duplication Register

This register separates textual duplication from semantic, fallback, and mapping
duplication. The optional `jscpd` console run did not produce clone blocks, so
the highest risks are semantic/source-of-truth duplication.

| Concept | Duplicated locations | Type | Current risk | Canonical owner recommendation | Suggested follow-up PS task | Blast radius |
|---|---|---|---|---|---|---|
| Best Rate selection | `src/services/rates.ts:468` `pickBestRate`; `src/routes/rates.ts:287` browse; `RateBrowserModal.tsx` sort/display; `OrdersView.tsx` `applyStrictBestRateResponse`; `v2-apiClient.ts:4143` combined best rate | Semantic/fallback | Table, panel, and modal can disagree on cheapest trusted rate. | Backend rate workflow service. | Backend Best-Rate Workflow DTO and UI Consumption | High |
| Rate request fingerprint/cache key | `src/services/rates.ts:387`; `src/services/shipping-workflow/rate-fingerprint.ts`; `OrdersView.tsx` request-key helpers | Mapping/semantic | Stale rates can look current if request identity ingredients diverge. | Backend fingerprint module only. | Remove Frontend Rate Fingerprint Authority | High |
| Selected-rate proof/persistence | `order_overrides.bestRateJson`; `shipments.selectedRateJson`; `OrdersView.tsx` proof builder; label/direct label proof guards | Semantic | Pre-label selected rate is partly UI state; final shipment snapshot is backend. | Shipping workflow selected-rate primitive and label operation. | Selected Rate Lifecycle DTO | High |
| `shippingProviderId` / carrier account / synthetic id mapping | `OrdersView.tsx` account helpers; `RateBrowserModal.tsx` account display; `v2-apiClient.ts` endpoint routing; `api/carriers/labels.ts`; `src/services/labels.ts` | Mapping | Wrong provider/account route can buy wrong label or show wrong account. | Backend provider account resolver. | Backend Provider Account Identity Resolver | High |
| Direct carrier scope/visibility | `src/lib/direct-carrier-scope.ts`; `src/services/rates.ts`; `api/carriers/rates.ts`; `api/carriers/labels.ts`; Rate Browser account list | Semantic | Unassigned/inactive/wrong-client account could quote or purchase if a layer misses guard. | Backend carrier scope guard before provider calls. | Direct Carrier Scope Certification Expansion | High |
| Label URL normalization and 4x6 output | `src/services/print-queue.ts`; `src/routes/print-queue.ts`; `src/lib/shipstation/labels.ts`; `v2-apiClient.ts` `openLabelPdf`; direct-carrier label output | Fallback/mapping | Invalid or object-shaped labels can break print queue or expose unsafe URL handling. | Backend label access/normalization service. | Label Access Descriptor Service | Medium |
| Print Queue vs Create+Print boundary | `OrdersView.tsx` route classifier; `src/services/print-queue.ts` create+queue; `src/services/labels.ts` create label; `api/carriers/labels.ts` direct queue/persistence | Semantic | Operator action can buy postage when intended queue path is ambiguous; partial queue failure is hard to reason about. | Shipping lifecycle operation with explicit `label_only`, `queue_existing`, `create_then_queue`. | Print Queue Operation State Machine | High |
| Order status vs shipment status vs marketplace confirmation | `orders.orderStatus`; `shipments.confirmationStatus`; `fulfillment_outbox.status`; marketplace reconciliation scripts; Orders UI badges | Semantic | Local shipped can be mistaken for marketplace/source confirmed. | Fulfillment confirmation lifecycle DTO. | Marketplace Confirmation Status Surface | High |
| Source/provider/store account identity | `orders.source*`; `store_orders`; `carrier_accounts`; `store_accounts`; connector orchestrators; Vercel provider functions | Mapping | Imported rows can drift between source tables and canonical orders. | Store connector orchestrator plus normalized source identity helper. | Store Source Identity Contract Certification | Medium |
| Inventory ledger vs balance/read model | `inventory.stockQty`; `inventory_ledger`; `InventoryView`; `inventory-parity.ts`; reporting metrics | Semantic | Cached/effective stock can be mistaken for movement audit truth. | Inventory service owns movement and effective stock DTO. | Inventory Effective Stock DTO Cleanup | Medium |
| Package defaults precedence | `product_defaults`; `sku_qty_dims`; `client_combo_package_defaults`; `order_overrides`; Orders panel package logic | Semantic | Saving dims/defaults can update wrong matching set or override immutable rows. | Package/default resolver service. | Package Default Precedence Resolver | Medium |
| Billing generated line items vs live reads | `billing_line_items`; `src/services/billing.ts`; `src/routes/billing.ts`; `BillingView`; repair scripts | Semantic | Invoice/detail may mix frozen lines with live shipment/order facts. | Billing generation service owns frozen invoice rows. | Billing Frozen Line Item Read Model | High |
| Frontend DTO transformations | `v2-apiClient.ts` normalization for orders/rates/labels/inventory/packages/billing | Mapping/fallback | UI can hide backend errors or silently remap missing fields. | Per-domain API clients or generated DTO schemas. | Split API Client By Domain | Medium |
| Product/SKU analytics | `orders.items`; `order_items`; dashboard/analysis/inventory/billing reads | Semantic | Analytics can read raw JSON while newer canonical item rows exist. | `order_items` and reporting service. | Order Items Analytics Source Migration | Medium |
| Carrier/service blocking rules | `src/services/rates.ts` eligibility, `RateBrowserModal.tsx` blocked reasons, `src/lib/shipping-service-eligibility.ts`, frontend markups utilities | Semantic | UI may display/select service differently than backend eligibility. | Backend eligibility service returns blocked reason/action. | Shipping Eligibility DTO Consolidation | Medium |

