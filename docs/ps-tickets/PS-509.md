# PS-509 — Freeze customer shipping money for ShipStation-synced shipments

**HARD BLOCKER for PS-508 cutover.** Weight 30% of PS-508. Status: **Hermes ACCEPT WITH CORRECTIONS at 0020492e — frozen contract ACCEPTED, packet 100%, PS-508 at 40%.** Implementation authorized; runtime writer acceptance gate mandatory before PS-508 cutover substrate — **packet drafted below, awaiting Hermes review**; 0% implemented.

---

## Packet item 1 — product-owner billability decision (DJ, 2026-08-19)

> **Bill them.** Attributed, positive-cost, non-void, non-return ShipStation-synced shipments receive customer shipping money.

Scope as decided: the decision covers the forward-looking candidate population (every new sync insert is priced since PS-381). It does **not** decide the 2,277 pre-PS-381 legacy rows (PS-508 historical handling), the 389 unattributed rows (no client to bill), or the external-label orders — canonically 1,917 at c851a1ea (no active shipment row; Portal presentation fix).

---

## THE FROZEN CONTRACT — v2 (Hermes blockers incorporated, awaiting re-acceptance)

Hermes ruled APPROVE WITH BLOCKERS (packet 72%) on v1. All five blockers are incorporated below; changed points are marked **[v2]**. Nothing is implemented.

**1. Canonical writer — one money authority, two trigger boundaries [v2].** All freezing delegates to a single domain service, `freezeSyncIngressCustomerShippingMoney`, owning eligibility, policy resolution, snapshot construction, version/provenance, named outcomes and idempotency. It is called from **both** trigger sites: the `upsertShipmentsBatch` INSERT transaction (driven off `insertedRows`), and **the orphan-link update (`order-sync.ts:298-308`), transactionally — link and freeze commit together**. Late attribution is the ordinary business path, not a review state; `late_attributed` survives only as a failure classification when the transactional freeze cannot complete.

**2. Eligibility — durable outcomes [v2].** Forward eligibility as v1 (`shipstation`, non-return, non-void, attributed, positive cost, billing active). Every evaluation **persists a versioned outcome keyed by shipment** — `frozen | no_order | no_client | billing_inactive | no_billable_cost | return | voided | test | needs_retry | needs_review` — so later config/attribution changes can never cause consumers to re-derive what happened at insertion from mutable state. Skips never write a money policy-version key; if outcome metadata shares `selected_rate_json` it stays outside the version key and is structurally guarded.

**3. Timing/provenance — three independent dimensions [v2].** Timing is not smuggled into the formula field:
- `customerShippingMoneyPolicyVersion = ps-509-v1`
- `customerRateSource = carrier_markup_customer_shipping_rate` (formula; `hugrab_shipping_rate_override` when the override fires)
- `customerShippingMoneyCaptureSource = shipstation_sync_ingestion` — **new field, required for ps-509-v1**, absent on historical v437/v508
- `rateCostSource = shipstation_sync_receipt_cost` — **not** `label_final_cost`, because ShipStation may revise cost later; at insertion this is an authoritative observed sync receipt, not proven-immutable final cost

The strict reader validates conditionally by version: v437 keeps historical optionality, v508 keeps the purchase-path contract, **v509 requires the capture source and sync receipt-cost source**. Unknown combinations classify as malformed — never silently normalized.

**4. Provider and house eligibility.** Unchanged from v1, approved: house is never for this ingress; house-rate input null always (the resolver OUTPUT remains a positive customer amount); post-hoc provider enrichment stays best-effort.

**5. Replay, partial payload, race.** Replay primitive unchanged, approved. **[v2]** Partial payloads that fail eligibility persist their named outcome (point 2) rather than merely skipping.

**6. Correction and void [v2].** Preserve frozen money; voids excluded downstream — approved. The promised detector now has a definition: a **`receipt_revised_after_freeze`** review class recording previous frozen selected cost, current postage, current insurance/other, signed and absolute delta, first-detected timestamp, shipment/client/source, and reconciliation state. Never auto-repriced, never overwritten; non-void corrected rows enter operator review; unresolved post-watermark corrections block broad activation. (Groundwork already in the audit: the receipt-vs-`selected_rate_cost` disagreement count measures the base rate before tuples exist.)

**7. Transaction, idempotency, failure semantics [v2 — reversed from v1].** Tuple and receipt commit atomically at both trigger sites. **The PS-508 savepoint rule is NOT copied here.** For a paid label, a freeze failure must not roll back the receipt (the postage is already sold). Sync ingestion is externally replayable: the upstream receipt survives in ShipStation: an aborted transaction leaves no row and retries as an INSERT next sync; a conflict-losing insert is suppressed and relies on the winner having written receipt and tuple atomically; replay of a committed row never re-freezes — but the savepoint shape would commit the shipment *without* a tuple, the next sync would take the UPDATE path which never freezes, and the gap would be **permanent**. Therefore: **an unexpected freeze failure on an eligible fresh insert aborts the insert transaction** (Hermes's preferred rule), letting sync retry the durable upstream receipt. Malformed/unknown snapshot handling stays review-only — an operator matter, distinct from infrastructure failure while constructing a new tuple.

**8. Historical non-goals.** Unchanged, approved — and the pre-PS-381 conclusion is now **PROVED by dates**: all 2,277 rows have positive receipt cost (zero/null: 0), and max(created_at) = 2026-07-03T01:15:00Z — three days before PS-381 was even committed (2026-07-06T03:04:44Z), which is itself the earliest bound on its deploy. 1,805 are attributed, and exactly those 1,805 carry read-time-fallback billing lines. Corrected canonical order-level counts (audit now reproduces shipped-label-display-state precedence exactly): active_label 5,308 · external_label **1,917** (was 1,930 under the approximation) · missing_shipment_sync 67 · voided_label 17. And the correction base rate: **0 of 2,748** stamped sync rows disagree with their receipt in 90 days — ShipStation post-ingestion cost revision is empirically rare.

**9. Acceptance evidence [v2].** v1's fixtures plus: late-attribution (insert unattributed → durable `no_order` outcome → link → freeze exactly once in the link transaction → replay link/sync → no repricing); unexpected-freeze-failure (insert aborts, retry succeeds, no tuple-less committed row); durable-outcome persistence; receipt-revision detection. Coverage must span **both** trigger boundaries: post-watermark eligible sync rows reach 100% `valid_ps509` whether frozen at insert or at link.

---

## Packet item 4 — canonical code trace (read-only, complete)

Traced at `0376ab2d`. No code changed.

### Call graph — a single funnel

```
sync-job-queue.ts:1555
  └─ syncShipments()                 shipment-sync.ts:787   (only caller)
       └─ upsertShipmentsBatch()     shipment-sync.ts:191   (only caller, line 890)
```

**`upsertShipmentsBatch` is the canonical sync insert/update owner.** One caller each way, no alternate entry point, and no raw-SQL `insert into shipments` anywhere in `src/`. This is the single seam PS-509 needs.

### Stage 1 — payload → scope filter

`partitionShipmentsByStoreScope` (:214) drops `EXCLUDED_STORE_IDS` at the ingest funnel, so excluded stores never create a row at all.

### Stage 2 — normalization

`shipmentValues(s, orderId, clientId)` (:142-171) builds the row. **What it does NOT produce matters as much as what it does:**

| written | not written |
|---|---|
| `cost` (`s.shipmentCost`) | `otherCost` |
| `carrierCode`, `serviceCode`, `trackingNumber` | `selectedRateCost` |
| `voided` (`s.voided`), `isReturn` | `selectedRateJson` |
| `source: 'shipstation'` (hardcoded) | |

### Stage 3 — order lookup and the existing-row split

`orderSourceIdentityOrLegacyPredicate` resolves the order. Rows then split on whether a row with the same `labelShipmentId` exists.

**UPDATE branch (:418-450)** applies field-level preservation — never null `orderId`/`clientId`, never rewrite `source`. That last one matters here: **a row PrepShip created keeps `source = 'prepship_v2'` even when sync updates it**, so sync updates can and do run over rows the PS-508 writer produced.

**INSERT branch (:451-474)** is the only place authoritative cost is stamped:

```ts
values.otherCost = toNumeric(s.insuranceCost) ?? '0.00';
values.selectedRateCost = resolveBillingSelectedRateCost({
  cost: values.cost, otherCost: values.otherCost, selectedRateJson: null,
})?.toFixed(2) ?? null;
```

Rows with an unresolvable order are recorded as `unattributed` (:467-473) rather than accepted silently.

### Stage 4 — first authoritative cost: INSERT ONLY

`selected_rate_cost` is written **on INSERT and never on UPDATE**, by deliberate design (:445-449): the update `SET` omits `otherCost`, so writing cost-only would drop a labelled row's insurance and change its billed total.

**Therefore the first authoritative cost exists exactly once per row, at insert.** That is the natural freeze point, and it is the direct analogue of `persistCreatedLabel`.

### Stage 5 — transaction boundary: **ASYMMETRIC**

This is the most consequential finding for PS-509.

| path | boundary |
|---|---|
| **INSERT** (:514) | inside `db.transaction`, chunked 500, with per-inserted-row work already running inside `tx` |
| **UPDATE** (:633-637) | **no transaction** — `db.update(...)` fired 3-at-a-time through `Promise.all`, each autocommitting independently |

PS-509 requires that "tuple and authoritative shipment receipt commit atomically." That is **achievable on the insert path** — the transaction exists and already hosts per-row work — and **not achievable on the update path as written**, where there is no transaction to join. Any tuple work on updates would be a separate autocommitted write, or would require introducing a transaction the concurrency-3 design deliberately avoids (Supabase pgbouncer tops out at 15 shared connections).

### Stage 6 — replay, correction, void

**Replay is already idempotent, and the primitive is strong.** `labelShipmentId` is UNIQUE, and the insert uses:

```ts
.onConflictDoNothing({ target: [shipments.labelShipmentId],
                       where: sql`${shipments.labelShipmentId} is not null` })
.returning({ ... })
```

`.returning()` yields **only genuinely inserted rows**. A replay produces an empty set, so a freeze driven off `insertedRows` cannot reprice — replay-safety comes free from the existing shape rather than needing new machinery.

**Correction is the open hazard.** `shipmentValues` writes `cost` on UPDATE but never `selected_rate_cost`. So when ShipStation revises a shipment's cost:

- `cost` changes;
- `selected_rate_cost` keeps its original value;
- any frozen tuple keeps its original value.

The carrier receipt and the frozen money then disagree, and nothing detects it. Today that is contained because no sync row carries a tuple. **After PS-509 it becomes a live divergence source**, and the packet must decide: re-freeze under a correction rule, flag for review, or explicitly accept the original as immutable.

**Void arrives the same way.** `voided: Boolean(s.voided)` is written on UPDATE, so a void can land on a row that already carries a tuple. Needs an explicit rule.

---

## Packet item 3 — population classification (read-only, complete)

Run initially at `6a270af4`; corrected queries executed as Actions run 32243003088 at `c851a1ea` (`0020492e` changed documentation only — the audit script is byte-identical). 90-day window, grouped counts, no row limit, session pinned `READ ONLY`.

### Sync shipment rows — `source = 'shipstation'`, 5,066 total

Buckets are mutually exclusive: the first matching branch wins, so a voided return counts once rather than inflating two counts.

| bucket | rows | share |
|---|---:|---:|
| `positive_cost_attributed` | **2,335** | 46.1% |
| `no_selected_cost` | **2,277** | 44.9% |
| `unattributed_no_order` | 389 | 7.7% |
| `voided` | 59 | 1.2% |
| `return` | 6 | 0.1% |

**Only ~46% of sync rows carry a positive `selected_rate_cost`.** The sync population is not broadly billable — roughly 53% could not be priced even if the owner wanted them billed, because they have no selected cost or no order to attribute to.

`positive_cost_attributed` is the **candidate** population. Candidate is not billable: source and cost cannot decide billability.

### Tuple presence

| bucket | rows |
|---|---:|
| `no_json_at_all` | **5,066** (all) |

Not one sync row carries `selected_rate_json` of any kind. Expected — no sync-path writer exists — and it confirms nothing is quietly writing there.

### Already represented in billing

| bucket | rows |
|---|---:|
| `line_exists_not_invoiced` | **3,800** |
| `no_billing_line` | 1,266 |
| `invoiced` | **0** |

**No sync row is invoiced.** 3,800 carry *draft* billing lines, which regenerate rather than being immutable history.

This materially softens PS-508's evidence-qualified historical handling: the "preserve immutable financial history" partition is **currently empty** for sync rows. That partition must still exist — invoicing happens on a cycle and the number will not stay zero — but nothing today needs preserving against a competing freeze.

### Order-level external/no-shipment population — 7,309 shipped orders

| bucket | rows | share |
|---|---:|---:|
| `has_active_shipment` | 5,308 | 72.6% |
| `external_label` (canonical, corrected run) | **1,917** | 26.2% |
| `missing_shipment_sync` | 67 | 0.9% |
| `voided_label` (corrected run) | 17 | 0.2% |

**1,917 shipped orders are external-label with no active shipment row (canonical precedence; voided_label 17, missing_shipment_sync 67).** No writer — PS-508's, PS-509's, or any future one — can ever give them a tuple. They must show an **explicit external state, not `Pending`**, which is a Portal presentation fix rather than a freeze problem, and it may account for a visible share of the original symptom.

### What these counts change

1. **The PS-509 candidate population is ~2,335, not ~5,066.**
2. **`no_selected_cost` at 2,277** — answered by item 6: a legacy stamping gap (pre-PS-381 rows with positive cost), not unpriceability. See below.
3. **The immutable-history partition is empty today**, so backfill risk is lower than assumed — but the partition must still be designed, because invoicing will change that.
4. **A Portal presentation fix is separable** from the freeze work and could reduce visible `Pending` without waiting on PS-509.

---

## Packet item 5 — contemporaneous facts at first ingestion (complete)

What the sync path actually possesses at the freeze point (the INSERT), from the `SSShipment` v1 payload, the schema, and a production run at `ff93e3b2`:

| fact | at the insert? | evidence |
|---|---|---|
| receipt cost | **yes — authoritative receipt cost observed at first sync ingestion; not proven immutable or final** | `shipmentCost` → `cost`; `selected_rate_cost` stamped on new rows since PS-381 (2026-07-06) |
| insurance | **yes** | `insuranceCost` → `otherCost` (`'0.00'` when absent) |
| label purchase timestamp | **yes** | payload `createDate` → `create_date` |
| client / store identity | **mostly** | order lookup at ingestion; 7.7% unattributed; store id via `advancedOptions.storeId` (used by the scope filter) |
| purchased-provider identity | **NO** | the v1 payload carries no provider/payer field; `provider_account_id` arrives via a later best-effort V2 enrichment keyed on tracking number — the same written-later-by-a-task-allowed-to-fail shape as the PS-508 house sidecar. Empirically it lands completely (5,066/5,066 resolved in the window), but never at the insert |
| customer policy (markup / HUGRAB / reference / house) | **present-day only** | `billing_config` / `order_overrides` are read-at-now; **no history table exists anywhere in `src/` or `drizzle/`**, so purchase-time policy is unprovable retroactively |

**Measured purchase → ingestion lag** (`created_at` − `create_date`): last-7-days **p50 138s, p90 281s, p99 777s (~13 min)** — consistent with the 3-minute scheduler. Whole-window p99 (~9.4h) reflects backlog walks and column-backfill artefacts, not steady state.

**Carrier distribution:** `stamps_com` 3,271 · `ups` 1,487 · `ups_walleted` 303 · `fedex_walleted` 5 — **no `shipp`**. House eligibility for this ingress is by construction never (a synced row is not a SHIPP-direct purchase), and the data agrees.

**Item-5 conclusion for the packet:** Hermes's bar for calling a freeze "purchase-time" is proving the policy facts *existed and were stable* at purchase. Existence at ingestion: yes. Stability across the gap: **unprovable** — no policy history exists — even though the measured gap is minutes. The packet should therefore take Hermes's explicit alternative branch: a **named `sync_ingestion` provenance with honest timing**, noting the steady-state gap is minutes. House inputs are moot for this ingress.

---

## Packet item 6 — redacted samples, and what they revealed (complete)

Three newest rows per eligibility class, shape only (no order/tracking identifiers), from the same run. Full table in the Actions log of run `32241251024`.

```
positive_cost_attributed:  cost 9.23  sel 9.23  order y  provider y  lag 62–252s   line N
no_selected_cost:          cost 5.88  sel null  order y  provider y  lag 382–846s  line y
return:                    cost 8.64  sel 8.64  order N  provider y  lag 11–529s   line N
unattributed_no_order:     cost 5.70  sel 5.70  order N  provider y  lag 103–326s  line N
voided:                    cost 7.75  sel 7.75  order y  provider y  lag 59–221s   line N
```

### The finding: `no_selected_cost` is a legacy stamping gap, not unpriceability

The samples show the bucket's rows carry **positive `cost` with `selected_rate_cost` null — and existing billing lines**. Superseding the earlier max-id inference (ids are not clocks), the grouped proof: 2,277 total / 2,277 positive receipt cost / 0 zero-null / max(create_date) 2026-07-03T01:08:39Z / max(created_at) 2026-07-03T01:15:00Z — all before PS-381 was committed (2026-07-06T03:04:44Z).

The bucket **stopped being produced before PS-381 existed** — every created_at precedes the commit that began stamping `selected_rate_cost` on new rows (new rows only, deliberately, to avoid rewriting historical truth). So the 2,277 rows are **pre-PS-381 history**: priced today by billing's read-time fallback (`resolveBillingSelectedRateCost` falls back to `cost + otherCost`), which is why they carry lines.

**This answers the packet's largest open question.** Consequences:

1. **Going forward, the candidate population is effectively all attributed, non-void, non-return sync rows** — new sync inserts all receive `selected_rate_cost`.
2. The 2,277 belong to PS-508's **evidence-qualified historical handling**, not to PS-509's writer — and they carry contemporaneous carrier-cost evidence (`cost`), which places them in the "evidence exists" partition rather than the unresolvable one.
3. `zero_or_negative_cost` did not appear as a bucket in production at all.

---

## What the trace settles

1. **Canonical owner identified** — `upsertShipmentsBatch`, single funnel, no alternate writers.
2. **The freeze point is the INSERT branch**, where the first authoritative cost is stamped and a transaction already exists.
3. **Replay safety is inherited** from `onConflictDoNothing` + `.returning()`.
4. **The update path cannot offer atomic commit** without a design change it currently avoids on purpose.
5. **Correction and void reach rows through UPDATE**, which is exactly the path that cannot freeze atomically — so those rules cannot simply mirror the insert rule.

## What the trace does NOT settle

- ~~Billability~~ — RESOLVED by item 1 (DJ, 2026-08-19): bill the forward population.
- ~~Contemporaneous policy facts~~ — settled by item 5: unprovable (no policy history table); the packet takes the named `sync_ingestion`-provenance branch.
- ~~Purchased-provider identity~~ — settled by item 5: absent at insert, arrives post-hoc via V2 enrichment; and no `shipp` carrier exists in the sync population, so house eligibility for this ingress is never.

## Note for PS-508

Sync UPDATE cannot clobber a frozen tuple **today**, because `shipmentValues` omits `selectedRateJson` and `selectedRateCost`. That is a property of the current helper, **not an enforced constraint** — adding either field to `shipmentValues` would silently overwrite frozen customer money. Worth a guard before cutover.
