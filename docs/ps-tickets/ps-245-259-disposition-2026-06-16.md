# PS-245 → PS-259 — disposition record (2026-06-16)

A 2026-06-15 implementation audit found the Card 0–14 security epic was reported "complete" when
each card had in fact landed **one high-value slice + a behavioral guard** (closing the named
exploit) without satisfying its full written acceptance matrix. DJ reviewed the audit on 2026-06-16
and chose a **Hybrid** disposition. This file is the DJ-approved record the auditor asked for
("finish OR record DJ-approved descope/follow-up cards").

## What each card actually shipped vs. its full DoD

| Card | Audit % | Slice that landed (closes the exploit) | Disposition |
|---|---|---|---|
| PS-245 (0) | 35% | lockdown file-fence `015435ef` | **DESCOPE** → PS-245-F (baseline/resolver/golden/CI harness) |
| PS-246 (1) | 20% | `financials:write` `4c277225` (+ fail-closed scope=PS-233, no-email-admin=PS-181) | **DESCOPE** → PS-246-F (app_metadata + RLS matrix + audit soak); STRICT_JWT_CLAIMS flip = DJ |
| PS-247 (2) | 45% | read-path scope (PS-233/240) | **FINISH** (cross-tenant matrix + concurrent-deduction) |
| PS-248 (3) | 50% | void/direct-label/test-label guards | **FINISH** (route scope + concurrent-buy + atomic commit) |
| PS-249 (4) | 65% | write-gate `d0ab5832` + storage-unique `7160a5dd` | **FINISH** (client-scope + txns + margin DTO) |
| PS-251 (6) | 35% | SSRF allowlist `77b13e17` | api/* OAuth tail owned by **PS-200** (in-progress) — not re-opened here |
| PS-252 (7) | 45% | products/locations + packages DELETE `1d491a5f`/`8273827a` | **FINISH** (route authz sweep) |
| PS-253 (8) | 35% | automation-rule lock `0918135c` | **FINISH** (outbox/confirm idempotency) |
| PS-254 (9) | 90% | escape/SQLi/onError `d8054eba` + secret-scan `20b1c3d6` | **FINISH** (CI-gate inclusion) |
| PS-255 (10) | 85% | ops dry-run gate `f9ea0468` | **FINISH** (CI-gate inclusion + ephemeral-DB smoke) |
| PS-256 (11) | 35% | durable rate-limiter `46982247` | follow-up (durable job-maps/worker-state) — not in this Hybrid pass |
| PS-257 (12) | 20% | `@ts-nocheck` ratchet `d1251b35` (freeze) | **DESCOPE** → PS-257-F (@ts-nocheck paydown) |
| PS-259 (14) | 50% | authz behavioral meta-ratchet `232fc889` (freeze) | freeze stays → PS-259-F (convert 16 substring guards) |

Cards 2/3/5 prerequisites (PS-247/248/250) and Card 13 (=PS-166) tracked separately.

## DJ-approved descope → follow-up cards (board should be updated)

### PS-245-F — Card 0 verification harness (the non-fence remainder)
Descoped from PS-245 because the lockdown fence (the high-value piece) shipped, and the rest is
heavy meta-infra whose golden-capture step is inherently **live/operational**, while baseline-diff
is already provided by `test:master:all-safe`.
Scope: `BASELINE.json` automation, the `cardId` verify-card resolver, 4 golden money-snapshots
(capture = live), CI integration. **Recommend: keep as a lower-priority infra card; golden-capture
needs a live run by DJ.**

### PS-246-F — Auth foundation soak (the non-permission remainder)
Descoped from PS-246 because the `financials:write` primitive shipped and the other foundation
items already landed under other cards (fail-closed client/store scope = PS-233; no-email-as-admin
= PS-181). Remaining: `app_metadata` role backfill, a behavioral RLS matrix test, and an
audit-mode soak. **DJ-operational tail:** flip `STRICT_JWT_CLAIMS=true` on Render after a canary.

### PS-257-F — `@ts-nocheck` paydown
Descoped from PS-257 because the ratchet (freeze at 94) shipped; actually removing `@ts-nocheck`
from 94 files + adding DTO types + shrinking the ceiling + render-smoke for the big views is its
own multi-slice epic, not a single card slice.

### PS-259-F — Convert the 16 substring authz guards to behavioral
The meta-ratchet (blocks NEW weak guards, ceiling 16) shipped; converting the existing 16
substring-only authz/scope guards to behavioral + de-wiring stale guards + repointing the
security/full-site profiles is the remaining paydown.

## Finish list (built in this Hybrid pass, one slice per commit, verified + triple-pushed)
PS-254/255 CI-gate inclusion → PS-247/248 (cross-tenant + concurrency) → PS-249 (client-scope) →
PS-252 (route sweep) → PS-253 (outbox/confirm idempotency). Lockdown-touching slices (PS-247/248
label/shipment write atomicity) will be flagged to DJ for the `unlock shipped data` override before
any locked-surface change; read-only scope gates that only add 404s are non-lockdown and ship first.
