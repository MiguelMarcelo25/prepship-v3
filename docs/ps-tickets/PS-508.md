> **SUPERSEDING HEADER — 2026-08-24 (do not remove; reconciles this document with the
> DJ-authorized implementation).** The Hermes re-audits of 2026-08-24 (FAIL 72% -> FAIL 82%)
> and the landed implementation override this document wherever they conflict:
>
> - **Topology is DARK-WRITE / GATED-READ**, formally ruled and documented at
>   `src/services/customer-shipping-money-cutover-gate.ts`. The writer freezes on every
>   controlled purchase unconditionally; Billing consumption is per-client gated
>   (`PS508_BILLING_FROZEN_TUPLE_CLIENTS`, default empty = off); the cutover boundary is
>   `PS508_BILLING_FROZEN_TUPLE_CUTOVER_AT` (empty = none, unparseable fails closed).
>   Any older "one gate covers writing and consumption" wording in this document is revised.
> - **W5/W6 are landed** on `prepshipv4-stable` (5e4a62dd, 314d4719, 5395d2dd, 73147dd3,
>   81666139): Billing delegates to `decideBillableShippingMoney`, the legacy resolver is a
>   lazy thunk reached only by gate-off or gated legacy_absent rows, held tuples surface as
>   visible `shipping_missing` review lines, and the real-generator PG17 proof
>   (`scripts/ps-508-billing-generates-frozen-line-pg17.ts`) runs in CI.
> - **Portal parity** is PR #29 on client-portal-prepship: suffix required for ps-508-v1 AND
>   ps-509-v1, negative fixtures, CP-041 pins, and the ps-437 lane explicitly arbitrated as
>   the historical return/replacement lane (suffix deliberately not required there).
> - The **lineage-column / cutover-table / divergence-record architecture** described below is
>   FUTURE work under discussion, not part of the audited blockers A/B/C and not required for
>   the canary. Hermes explicitly ruled these are not current blockers.
> - The shadow-parity thresholds and population gates below remain good canary guidance and
>   feed the operator checklist on the Trello card.

---
# PS-508 — Freeze ordinary outbound customer shipping money, and cut Billing over to it

> ⚠️ **SUPERSEDED (2026-08-24 — see the header above).** Hermes round-3 scored the ticket 85% with W5/W6 LANDED; Billing precedence is
> implemented, not 0%, and PS-509 shipped. The line below is historical.

**Status: 30% complete.** Parent business outcome. Cutover blocked by PS-509.

**Trello:** [PS-508](https://trello.com/c/FrlYTRvb) (Lawrence In Progress) · **Child/blocker:** [PS-509](https://trello.com/c/WLB0ADDW) (Lawrence To-Do)
**HEAD at last review:** `750b6c3f` on `prepshipv4-stable` · **Baseline:** `0a630871`

---

## The symptom this ticket exists to close

The operator UI shows a customer shipping rate (e.g. `$9.04` on order `200015261150723`). The Client Portal shows **`Pending`** for the same order.

Same database, same `shipments` row. The operator view **recomputes** live from `selected_rate_json` plus billing policy; the portal reads **only** the version-gated frozen tuple and has nothing to show. It is a coverage gap, not a disagreement about the amount. `Ext. Label` rows correctly show no rate on either side.

**PS-508 is not done until that `Pending` is gone for ordinary outbound shipments.** No amount of writer correctness substitutes for that.

---

> ⚠️ **SUPERSEDED (2026-08-24 — see the header above).** These weights/completions predate the landed implementation; the current score
> and per-workstream breakdown live in the Hermes round-3 audit (85%, 2026-08-24).

## Weighting (Hermes, revised at `1c9313ad`)

| Workstream | Weight | Complete |
|---|---:|---:|
| Staged `persistCreatedLabel` writer | 10% | **10%** |
| Classifier, coverage audit, watermark, shadow infrastructure | 15% | **15%** |
| Malformed-version handling | 5% | **5%** |
| **PS-509 ShipStation sync ingress** | **30%** | 0% |
| Billing shadow + tuple precedence, finalized-period proof | 15% | 0% |
| Evidence-qualified historical handling | 8% | 0% |
| Portal acceptance | 8% | 0% |
| Observation cycle, fail-closed transition, acceptance | 9% | 0% |
| **Total** | **100%** | **30%** |

The staged writer fell from 15% to 10% at the third review: it is the correct canonical implementation and the path could resume, but production evidence shows it has protected **none** of the observed ordinary traffic for roughly four weeks. PS-509 rose from 20% to 30% because it is now the only ingress capable of moving the symptom.

---

## Complete

### Staged purchase-path writer — PASS 94%
Freeze at `persistCreatedLabel` (the canonical outbound producer, 2 code-level call sites), inside the committed ship transaction. Includes, after two BLOCK rulings:

- **Purchased-provider gate.** House money follows the PURCHASE, not the stamp. `captureRealizedHouseMargin` is gated on `directProviderKey === 'shipp'`; deriving from `best_rate_json` + opt-in alone was a *different* eligibility boundary, and a stale SHIPP stamp plus a non-SHIPP purchase froze house money where billing applies carrier markup (~$1.00/shipment in the reproduction).
- **SAVEPOINT, not a bare try/catch.** A failed PostgreSQL statement aborts the whole transaction; catching the JS exception does not restore it. Proven by mutation: removing the savepoint yields `25P02`.
- **House rate derived in-transaction**, because `order_competitive_rate` does not exist at label-commit — `captureRealizedHouseMargin` runs `timer.background` *after* the ship transaction commits and keys on a `shipment_id` that does not exist until the INSERT returns.
- Two coexisting policy versions (`ps-437-v1`, `ps-508-v1`), house provenance as a distinct third `customerRateSource`, and `billingDescriptionSuffix` as an eighth field (billing consumes it, and description participates in the duplicate-suppression unique index).

### Classifier, audit and watermark infrastructure
- **Five-state classifier** — `valid_ps508` / `valid_ps437` / `legacy_absent` / `malformed_known_version` / `unknown_version`. Safety property: **only `legacy_absent` may take the recompute fallback.**
- **Read-only coverage + shadow audit**, run as a Render one-off job (not a workstation), session pinned `READ ONLY` at the server, operator from authenticated `github.actor`, no apply mode, report transcribed into the Actions log.
- **Cutover watermark split**, with an activation blocker when a post-watermark row lacks a tuple.
- **Deterministic per-source timeline** — `count, min/max(create_date), min/max(id)`, no row limit.

### Malformed-version handling — accepted
`freezeOutboundCustomerShippingMoney` returns a discriminated `OutboundFreezeOutcome` instead of `Frozen | null`. A malformed tuple used to return `null` — byte-identical to "skipped, not applicable" — so a row written WRONG was indistinguishable from one correctly left alone, and billing would have recomputed it as ordinary legacy history. Neither `malformed_known_version` nor `unknown_version` is ever rewritten: the first is evidence of a writer defect, the second may belong to a newer policy. `OutboundFreezeSkipReason` is a closed union of nine values.

---

## The production evidence that reframed the ticket

90-day source mix (grouped count, no row limit, read-only production audit):

| source | rows | first | **last** | writer |
|---|---:|---|---|---|
| `shipstation` | 5,066 | 2026-05-21 | **2026-08-18** | sync ingestion — **no tuple writer** |
| `prepship_v2` | 778 | 2026-05-22 | **2026-07-24** | `persistCreatedLabel` |
| `shipp` | 246 | 2026-06-04 | **2026-07-07** | `persistCreatedLabel` |
| `walmart_shipping` | 53 | 2026-05-23 | **2026-07-07** | `persistCreatedLabel` |
| `test_offline` | 5 | 2026-07-14 | 2026-07-27 | excluded |
| `prepship_return_v2` | 5 | 2026-07-16 | 2026-07-20 | return freeze (PS-437) |
| `shipstation_fulfillment` | 2 | 2026-05-27 | 2026-05-29 | — |

**Purchase-path ingress stopped between 2026-07-07 and 2026-07-24. Only ShipStation sync is still producing rows.**

Two caveats that must travel with this table:

1. **`min(id)`/`max(id)` are computed independently of `min/max(create_date)`** — they are not the IDs belonging to those dates.
2. **This proves ShipStation is 100% of recent row *ingress*. It does NOT prove 100% of those rows should be *billed*.** That depends on the unanswered billability decision.

An earlier claim that purchasing stopped on 2026-08-10 was **wrong**: it was inferred from ID ordering, and the newest-500 sample only reached back to Aug 10, so the boundary of the sample was reported as the cessation date.

---

> ⚠️ **SUPERSEDED (2026-08-24 — see the header above).** This ordering predates the landed W5/W6 cutover. PS-509 is DEPLOYED, Billing is
> cut over behind the default-off gate, and the actual remaining work is the canary
> checklist in the CURRENT-STATE CHECKLIST below — not the steps in this section.

## What remains, in mandated order

Each step must be safe if the next never happens.

### 1. PS-509 architecture/evidence packet — **the single next step**

No code. The packet is complete only when it freezes:

1. product-owner billability policy;
2. the canonical sync writer and all callers;
3. eligible and excluded populations, with production counts;
4. honest snapshot timing — purchase-time only when proved, otherwise explicitly named **sync-ingestion-time** provenance;
5. authoritative purchased-provider and house eligibility;
6. first-ingestion, replay, partial payload, race, correction and void rules;
7. transaction and idempotency boundaries;
8. malformed/unknown snapshot handling;
9. end-to-end acceptance evidence, sync ingestion → Billing shadow → Portal projection.

**May proceed now, read-only:** the code trace `ShipStation payload → normalization → canonical insert/update owner → first authoritative cost → replay/correction/void → transaction commit`, and population classification counts.

**Blocked on the product owner:** which synced shipments are intended to receive a customer shipping charge, distinguishing (a) positive authoritative carrier-cost billable shipments, (b) genuine external labels with no customer-rate evidence, (c) provisional/incomplete receipts, (d) voided or corrected labels, (e) returns/replacements/tests. **Source name alone cannot decide billability.**

### 2. PS-509 implementation
Only after the packet is accepted. Canonical behaviour: one sync-ingress owner writes the tuple; incomplete or provisional receipts do not poison the one-shot boundary; replay does not reprice; correction and void behaviour is explicit; unknown and malformed versions stay review-only; provider/house eligibility uses authoritative purchase evidence; tuple and authoritative receipt commit atomically.

### 3. PS-509 deployed runtime acceptance

**The gate between implementation and any Billing work.** Exact-SHA CI and local fixtures are *not* sufficient, because PS-509 owns the only currently active ingress — without this, Billing shadow could begin against no post-cutover tuples, only fixture-created ones, or a writer the real sync worker never reaches.

Required evidence: exact deployed SHA; at least one representative eligible ShipStation sync produces a valid tuple; replay produces no repricing or duplicate; a partial/incomplete payload stays safely retryable; an external/non-billable label stays explicitly excluded; correction and void behaviour matches the packet; **no malformed or unknown tuples introduced**; post-deploy production coverage measured by eligibility class; tuple creation and shipment persistence share the required transaction boundary.

> ⚠️ **SUPERSEDED (2026-08-24 — see the header above).** The lineage-column / cutover-table / divergence-record substrate is FUTURE work.
> Hermes explicitly ruled it is NOT a canary prerequisite; the dark-write/gated-read
> topology shipped without it.

### 4. Cutover substrate — deployed INERT, before shadow

Three durable mechanisms, all deployed inactive, so shadow results and later activation are auditable rather than reconstructed.

**A. Dedicated Billing-line customer-money lineage.**
`billing_line_items.billing_policy_version` **cannot** carry this. It is owned by billing-*calendar* policy and migration `drizzle/0071_billing_weekend_rollforward.sql:18-27` constrains it by CHECK to `legacy_calendar_v1` / `weekday_weekend_rollforward_v2` (or NULL) — writing `ps-508-v1` there is a **constraint violation**, not merely a semantic overload. Add dedicated migration-owned columns: `customer_shipping_money_policy_version`, `customer_shipping_money_source`, `customer_shipping_money_description_suffix`.

Invariant for tuple-priced shipping lines: **version, provenance and suffix are all present or all absent.** The persisted amount must equal the tuple amount after canonical rounding; the rendered description must use the persisted suffix; provenance and version must come from the same classified tuple; and a finalized line must **never** recover its lineage later by joining mutable shipment JSON — a finalized line is financial history and must stay legible after the tuple is repaired, the accepted versions change, the JSON schema evolves, or the code that wrote it is long gone.

**B. Durable cutover-control record.** A migration-owned table — not runtime config, not the policy version:

```
customer_shipping_money_cutovers
  contract/scope · consumer · state: shadow | active | fail_closed
  accepted_policy_versions · activated_at · shipment_id_high_watermark
  activated_by · prepship_deploy_sha · portal_deploy_sha
```

Config is wrong because it can differ between workers, change on redeploy, roll back independently, vanish from historical evidence, and let Billing, Portal and audit classify the same row differently. The policy version is wrong because it answers *which contract priced this tuple*, not *from which boundary did a missing tuple become illegal* — conflating them blocks staged activation and any future cutover on the same version.

Activation must capture timestamp and high-watermark ID **in one transaction**, and all consumers must call one canonical backend resolver returning `pre_cutover_legacy` | `post_cutover_required`. Portal receives the classification; it never owns cutover math.

**C. Durable divergence records** — structured and queryable, with reconciliation state. Not console logs.

### 5. Billing shadow + tuple precedence
Deployed **disabled**, run in shadow through complete invoice generation including finalized-order reconciliation. Owner decision on record: **bill the tuple, log divergence** — but "log" must mean durable structured records with thresholds and a circuit breaker, not a console warning, or a systematic error accumulates across an invoice period. Billing must consume the tuple **atomically** — amount *and* `billingDescriptionSuffix` *and* provenance *and* policy version — or line money and line identity come from different policies.

### 6. Evidence-qualified historical handling
**Not a blanket backfill.** Recomputing old customer money from present-day policy manufactures a label-time fact that may never have been true. Partition: exact contemporaneous evidence (may be repaired); an existing generated/finalized billing line (preserve as financial history); no contemporaneous evidence (do not backfill — retain explicit legacy recompute or `Pending`). Must refuse returns, replacements, voids, external labels, tests, bundle children and anything already invoiced.

### 7. Billing activation
**Billing leads Portal.** Portal-first would expose tuple money to customers while invoices still come from recomputation — two independently activated authorities.

### 8. Portal acceptance
Four sites pin the `ps-437-v1` literal (TS reader, SQL gate, HTTP-boundary validator, audit script); none import the constant. Returns and replacements must remain valid under `ps-437-v1` with regression proof.

### 9. Observation cycle
At least one complete billing generation/finalization cycle, with an operator-reviewed reconciliation queue and a flag-off rollback path.

### 10. Fail-closed — split three ways
Writer fail-closed for new labels; Billing fail-closed for post-watermark missing/invalid snapshots; **explicit legacy fallback for pre-watermark absence.** A global "missing tuple = fail" rule would strand historical rows that can never receive an honest snapshot.

Guards designed to fail at cutover and requiring faithful re-anchoring: `ps-437-customer-shipping-money-guard.ts:127`, plus the two PS-508 checks asserting billing still recomputes and nothing opts into v2.

---

## Parent acceptance test — what "done" means

"`Pending` is gone" is necessary but imprecise. The acceptance population is **all eligible post-cutover ordinary outbound shipments, across every active ingress** — controlled purchase paths, ShipStation sync, and any future ingress routed through the same canonical eligibility contract.

Explicitly excluded: genuine external labels without customer-rate evidence, returns, replacements, voids, tests, provisional/incomplete receipts, bundle children with no independent shipping charge, and any other owner-approved non-billable class. **For those, Portal must show an explicit non-billable/external state — never a misleading `Pending`.**

The 82% / 100%-of-recent-ingress share is **not** the threshold. Coverage must be **100% of the eligible population**, whatever the source mix.

> ⚠️ **SUPERSEDED (2026-08-24 — see the header above).** Persisted Billing lineage is future architecture, not an acceptance requirement;
> the round-3 acceptance conditions are the canary packet + operator checklist below.

For every eligible post-cutover shipment:

1. a valid accepted tuple exists at the canonical persistence boundary;
2. Billing consumes **that exact tuple**, not a recomputation;
3. the Billing line records customer-money version and provenance;
4. before invoicing, Portal shows the tuple amount and not `Pending`;
5. after generation/finalization, Portal shows the Billing line amount;
6. pre-invoice and finalized amounts match;
7. description/suffix identity comes from the same tuple as the money;
8. replay does not reprice or duplicate;
9. corrections and voids follow the accepted PS-509 contract;
10. no eligible post-watermark row falls into legacy recomputation.

### Population gate, after one complete finalization cycle

Every one of these must hold:

| measure | required |
|---|---|
| eligible post-watermark tuple coverage | 100% |
| eligible post-watermark Portal `Pending` | 0 |
| malformed / unknown snapshots | 0 |
| tuple-priced Billing lines missing lineage | 0 |
| unexplained cent-level divergence | 0 |
| duplicate shipping lines | 0 |
| unauthorized finalized-period mutations | 0 |
| return / replacement regressions | 0 |

Pre-watermark historical rows may retain an explicit legacy fallback or unresolved state. They are **not** grounds for calling the writer failed — but they must never be presented as label-time frozen facts.

---

## Divergence thresholds

The owner's decision is **bill the frozen tuple, not the recomputation**, so raw non-zero divergence cannot itself be the stop condition — a legitimate policy change produces divergence by design. Two tiers:

**Hard blockers (zero tolerance) — stop rollout if the activation population contains any:** malformed known snapshot; unknown snapshot; missing tuple on an eligible post-watermark row; amount/suffix/version/provenance atomicity mismatch; unintended mutation or adjustment against finalized history; unexplained divergence over one cent after canonical rounding. Before activation, **every** non-zero expected divergence needs a classified reason and approved disposition.

**Concentration circuit breaker — pause expansion and require review when, in one billing run:** a single shipment diverges by more than $5 absolute; a client×source cohort diverges on more than 1% of compared rows; a client×source cohort reaches $25 absolute; or aggregate absolute divergence reaches $100. These are rollout-review thresholds, **not** instructions to recompute instead of billing the tuple.

Evaluate in three forms: **client×source cohort** (primary — catches one broken client policy or one sync writer, and stops healthy volume hiding a concentrated defect), per-client and per-source rollups (shows whether the fault follows configuration or ingress implementation), and aggregate (backstop only — never sufficient alone, because positive and negative deltas cancel). Retain **both** signed and absolute dollars: breakers use absolute exposure, signed values explain direction.

---

## Returns and replacements stay on `ps-437-v1`

Not migrated. PS-508 changes ordinary outbound coverage and consumption; it does not require rewriting returns or replacements so every row shares one version label. Existing v1 tuples stay readable, new return/replacement workflows may keep writing v1, and Billing and Portal accept v1 for those workflow classes — with regression proof that amount, suffix, provenance and identity are unchanged. "Permanently" would be too strong: a future change may introduce another version, but as a separate versioned contract, not hidden inside PS-508.

---

## Point of no return

**The first invoice finalized under tuple precedence.** Before that, everything is a flag flip. After it, rollback requires credit/debit notes against immutable financial history.

Evidence required before it: exact-SHA coverage manifest; counts by snapshot class; tuple-vs-recompute delta with per-client and per-source grouping; an approved disposition for every non-zero divergence; **zero malformed/unknown snapshots** in the activation population; a full Billing generation dry run including finalized reconciliation, proving no unintended credits or debits; behavioural tests for amount and suffix including HUGRAB and house paths; return/replacement regression proof; Portal parity proof; deployed PrepShip and Portal SHAs; named operator approval and a documented rollback procedure.

---

## Open questions

1. **Is the positive-cost ShipStation-sync population supposed to receive customer shipping money?** Blocks the PS-509 packet.
2. **Was the move to ShipStation-side purchasing between 2026-07-07 and 2026-07-24 deliberate?** If permanent, the staged writer guards a path no longer used and PS-509 becomes effectively the whole outcome.

## Known gaps

- The staged writer has **no post-watermark production execution** — its live behaviour is unproven.
- Concurrent double-purchase behaviour is unproven; PGlite is single-backend.
- The eligibility gate is pinned structurally, not driven through `persistCreatedLabel` end to end.
- `args.tx ?? db` remains permissive at `labels.ts` — must be tightened before the fail-closed step.
- `scripts/` is **not typechecked** (`tsconfig.json` includes only `src/**`), so guard scripts have no type coverage.
- This repo's CI uses `cancel-in-progress: false`: pushes queue rather than supersede, and a run-level `cancelled` can hide a passing guard-pack job. **Read CI at job level.**

## Unrelated, acknowledged

Production Watchdog failing on `Inventory claim backlog` — `state: alarm`, `inventory_claim.fixed_regression.shipment_sync`, `reviewCount: 3553`, `oldestAgeDays: 34`. Pre-existing, own runbook (`docs/runbooks/ps-431-production-self-healing.md`), product owner chose to leave it. Not caused or modified by PS-508.

---

## CURRENT-STATE CHECKLIST (authoritative, 2026-08-24)

- **Topology:** dark-write / gated-read. Writer freezes unconditionally; Billing consumption is
  per-client gated (`PS508_BILLING_FROZEN_TUPLE_CLIENTS`, default empty=off); boundary is
  `PS508_BILLING_FROZEN_TUPLE_CUTOVER_AT` (empty=none, unparseable fails closed).
- **Implemented and CI-green on prepshipv4-stable:** W5 gated Billing consumption, W6 boundary,
  behavioural + real-generator PG17 proofs, 12-mutant reproducible manifest (SOT-pack enrolled),
  canary evidence packet v2 (`npm run canary:ps-508-packet`) with its own PG17 smoke.
- **Portal (PR #29):** ps-508/ps-509 suffix parity, negative fixtures, CP-041 pins, ps-437
  non-return lane fail-closed. Awaiting DJ review/merge.
- **Actual canary prerequisites:** merge PR #29 · direct Render env readback · pilot client ·
  exact UTC boundary · packet v2 in --mode activation with all identity fields · zero
  mismatches per the packet's PASS verdict, which enforces: nonzero eligible rows, full
  comparison, 100% eligible tuple coverage (any post-boundary missing/malformed/unknown row
  fails even when correctly held), cohort coverage with waivers restricted to non-derivable
  cohorts, bound deployment identity (40-hex SHAs, toolGitSha == api-sha == live /health
  commitSha), and portal-mirror freshness.
- **Packet limitations (recorded):** legacy rows are OBSERVED, not validated; non-derivable
  cohorts (insurance-adjusted, markup-change, direct-carrier) need operator evidence or a
  recorded waiver; the Portal predicate is an embedded SQL mirror pinned to the deployed
  Portal SHA — activation refuses if they diverge.
- **Future work (not activation prerequisites):** durable Billing-line lineage columns,
  migration-owned cutover table, divergence records.
