# PrepShip v4 — Full-System Audit & Optimization Roadmap

**Date:** 2026-07-13 · **Repo at:** `b6c31fb1` (prepshipv4-stable) · **Method:** six parallel deep audits (print queue, rates, syncing, billing, frontend, backend platform) + live prod-DB measurement (Supabase "Prepship", pg_stat window since 2026-05-05).
**Report artifact:** <https://claude.ai/code/artifact/5df7955d-151c-4d20-9afb-e40551436ec1>
**No code was modified during the audit.** Every claim carries a file/line or prod-DB citation so it can be re-verified before coding.

**Totals: 76 findings — 4 Critical · 23 High · 30 Medium · 13 Low · plus 6 measured DB hotspots.**

---

## Executive verdict

The deliberately-built parts of the system are genuinely good: backend-owned rate ranking and proof (PS-313), the fulfillment outbox, purchase locks, pg-boss sync lanes with advisory locks, billing's delete-and-rebuild generation, comprehensive route-level code splitting. **Almost every serious defect lives at a seam** — two features composed unsafely, two writers sharing one surface on different money bases, a retry horizon outliving a lock TTL, or legacy mass predating the current doctrine (the 4,937-line orders route, 2,539 lines of `@ts-nocheck` handlers, in-memory job state under a two-process deployment).

> **Lockdown gate:** roughly a third of Phase 1 touches shipped/cancelled-locked surfaces (`labels.ts`, `shipment-sync.ts` writes, `fulfillment-deductions.ts`, `shipments` schema, the confirmation sweep). Per CLAUDE.md these need DJ to type `unlock shipped data` (the active PS-016–021 override covers only some print-queue/OrdersView work). Items below are tagged **[LOCK]**.

---

## Measured DB evidence (69-day window)

| ID | Measurement | Meaning |
|----|-------------|---------|
| M1 | Orders list query: **~3.0M calls, ~22h cumulative DB time** (43k/day, one every 2s) | FE polling fleet (10 timers, 2.5s recalc loop) × remount-refetch on every view switch |
| M2 | Shipments UPDATE: **1.25M calls → 3,508 rows changed** (99.7% no-op); no btree on `tracking_number` → 2.35M seq scans / 56B tuples read | Provider-account binding loop re-runs per shipment per sync tick forever |
| M3 | Watchdog queries at **2.5–3.4s each**: `NOT EXISTS (… order_id = id OR order_number = order_number …)` defeats both partial indexes | Every externally-shipped health check seq-scans shipments per row |
| M4 | **`orders` = 502MB for 70k rows** (~7KB/row raw JSONB); caches add 205MB with no eviction (direct_carrier_rate_cache 103 + analytics 65 + rate_cache 37) | The DB-size driver behind the 2026-07-13 read-only incident (DB 1005→1021MB in one day) |
| M5 | **6 tables exposed via PostgREST with RLS disabled**: returns, return_items, return_inspections, return_activity_events, return_inspection_media, client_portal_audit_logs | Anyone with the anon key can read/write them via REST |
| M6 | Unindexed FKs (fulfillment_outbox, billing_box_resolutions, shipment_bundles, order_competitive_rate, return_items, returns); `billing_manual_overrides` has no PK; ~25 never-used indexes | Free wins during any migration pass |

---

## The four criticals

### C1 — Label purchase POST retries blindly on 5xx: one click can buy up to 5 labels
- **Evidence:** `src/lib/shipstation/client.ts:97,130-142` (5xx → retry, method-agnostic, maxRetries default 5); `src/lib/shipstation/labels.ts:250-257` (`ssCreateLabel` POSTs with no retry override, no idempotency key). Reached via `src/services/print-queue.ts:1243-1247` → `src/services/labels.ts:2217-2240`.
- **Impact:** a 502/504 after ShipStation actually created the label re-buys it up to 4 more times; only the last response is persisted — earlier labels are invisible paid orphans.
- **Fix:** `maxRetries: 1` on the purchase call; treat post-timeout/5xx outcomes as "unknown" and reconcile via `/v2/labels` before any re-send.
- **Effort:** S · **[money path — DJ review]**

### C2 — Crash between provider purchase and DB persist auto-rebuys postage
- **Evidence:** buy at `src/services/labels.ts:2217-2254`, persist transaction after (`:2261-2308`); lock TTL 10min (`src/lib/label-purchase-lock.ts:14`), expired-lock takeover `:62-68`; pg-boss `expireInSeconds: 30*60`, `retryLimit: 1` (`src/services/print-queue-worker.ts:59-66,109-117`); retry re-dispatches orders without results (`print-queue.ts:1603-1612`); PS-288 recovery needs a local shipments row to match — after crash-before-persist there is none.
- **Impact:** worker killed inside the buy→persist window ⇒ postage spent with zero local record; 30min later pg-boss re-delivers, the 10-min lock is gone, order repurchased automatically. Same window for direct carriers.
- **Fix (interim):** raise `LABEL_PURCHASE_LOCK_TTL_SECONDS` above the pg-boss retry horizon (fail closed as `LABEL_PURCHASE_IN_PROGRESS`). **Fix (complete):** durable purchase-intent row written before the provider call, finalized with the persist; unresolved intents force reconciliation, never a re-buy.
- **Effort:** S (interim) / L (complete) · **[LOCK]**

### C3 — Confirmation sweep re-notifies marketplaces for shipments ShipStation itself created
- **Evidence:** `src/services/fulfillment/outbox.ts:477-514` (sweep selects on `label_url IS NOT NULL AND confirmation_status IS NULL`, **no source filter**); `src/services/shipment-label-url-enrich.ts:36-50` (PS-286 backfills label_url onto ShipStation-sourced rows too); dispatch succeeds via `ssMarkOrderShippedV1` with `notifySalesChannel:true` (`src/connectors/store/shipstation.ts:270-297`).
- **Impact:** orders shipped inside ShipStation get a second marketplace ship-notification from PrepShip within 72h — duplicate buyer emails/channel updates.
- **Fix:** `AND s.source IN ('prepship','prepship_v2','test_offline')` in the sweep (or stamp `confirmation_status='not_required'` on SS-sourced inserts).
- **Effort:** S · **[LOCK]**

### C4 — Rate cache key omits ship-from origin: cross-origin cache poisoning up to purchasable proof
- **Evidence:** `src/services/rates.ts:701-726` (cache key — no origin), `src/services/shipping-workflow/rate-fingerprint.ts:175-208` (no origin field); PS-291 made origin operator-selectable (`rate-ship-from-origin.ts:34-79`) and origin changes quotes (`rates.ts:1487`); HTTP dedupe key shares the same gap (`rates.ts:1231,1295`).
- **Impact:** manual rate-shop from a custom origin writes cache rows that order-scoped rating reads as truth; wrong-origin prices can persist as Best Rate and finalize into purchasable proof.
- **Fix:** add `sf=<zip>|<country>` to the fingerprint from resolved ship-from; bump `RATE_CACHE_VERSION`. ~15 lines, two files.
- **Effort:** S

---

## Findings reference by subsystem

Severity key: 🔴 High · 🟡 Medium · ⚪ Low. (Criticals above.) Effort: S <1d · M 1–3d · L >3d.

### Print queue (PQ)

| ID | Sev | Finding | Fix | Effort | Lock |
|----|-----|---------|-----|--------|------|
| PQ-1 | 🔴 | 10-min timeout doesn't cancel work; pg-boss retry re-enters same in-memory job while first run is mid-flight (`with-deadline.ts:15-38`, `print-queue.ts:1600-1612`) | Cooperative cancel flag + per-jobId re-entry guard | M | yes |
| PQ-2 | 🔴 | Send-job recovery only at worker boot, silently deduped by 6-h singleton, no attempt cap — stranded jobs & poison crash-loops (`print-queue-worker.ts:168-216`) | Periodic reaper + persist "interrupted" on dedupe + attempt counter | M | no |
| PQ-3 | 🔴 | Capacity math: 1000-order jobs vs 10-min deadline, 1 retry, single head-of-line queue (`routes/print-queue.ts:582-623`, `env.ts:193`) | Chunk into ~100-order jobs, sequenced singleton keys | M | yes |
| PQ-4 | 🔴 | Merge-job durable state = last-writer-wins singleton; restart mid-merge = "running" forever; final PDF persist fire-and-forget (`print-queue.ts:260,604-620,2369-2390`) | Per-jobId snapshots + staleness derivation + await terminal persists | M | yes |
| PQ-5 | 🟡 | Transient provider outages classified terminal; circuit breaker cascades batch to failed_terminal (`rate-fingerprint.ts:493-511`, `print-queue.ts:1656-1679`) | Classify pre-purchase transport errors retryable | M | yes |
| PQ-6 | 🟡 | Worker job map never pruned → OOM over days (`print-queue.ts:1600` vs `:676-688`) | Evict at end of worker run | S | yes |
| PQ-7 | 🟡 | O(n²) snapshot rewrites (full job JSON per order); items written twice at start (`print-queue.ts:1682-1686`, `queue-send-job-store.ts:100-196`) | Counters-only progress UPDATE; batch item upsert | M | part |
| PQ-8 | 🟡 | `/print` unbounded entry list; chunk PDFs pinned in memory 30min (`routes/print-queue.ts:903-911`) | `.max(500)` + release persisted chunks | S | yes |
| PQ-9 | ⚪ | addToQueue finalized-row TOCTOU can revive a printed row (`print-queue.ts:1434-1485`) | Guard in the UPSERT `setWhere` | S | yes |
| PQ-10 | ⚪ | New supervised pg-boss instance per enqueue; status route 404s on slow reads (`print-queue-worker.ts:53-133`, `routes/print-queue.ts:708-753`) | Shared enqueue boss; 503 not 404 | S | part |

### Rates (R)

| ID | Sev | Finding | Fix | Effort | Lock |
|----|-----|---------|-----|--------|------|
| R-1 | 🔴 | Backfill persists best rate on different money basis than browse — proof keys don't resolve in own snapshot → spurious "Rate changed or expired" for marked carriers (`rates-backfill.ts:814-848`, `rate-quote-snapshot-store.ts:113`) | Finalize from marked cheapest (mirror browse); raw-restore only the persisted JSON | M | no |
| R-2 | 🔴 | `rate_cache.bestRate` re-picked on raw basis by repair path & eligibility sanitizer — markup-before-ranking violated (`rates.ts:1925-1933,848-850` vs `:1763`) | Apply markups before every re-pick | S–M | no |
| R-3 | 🔴 | Limiter per-process, not per-API-key: 2 processes = 2×160/min vs one key; labels/sync/rating share one FIFO bucket; durable bucket built but unwired for v2 (`lib/shipstation/client.ts:8-60`) | Per-key buckets; wire durable bucket; one admission point with priority | M | no |
| R-4 | 🔴 | Timeouts race but never abort — zombies retry ~7.5min burning shared budget (`rates-combined.ts:239-256`, `with-timeout-retry.ts:29-37`) | Thread AbortSignal + timeoutMs into ssRequest | M | no |
| R-5 | 🟡 | Single-flights & ratchet process-local/non-atomic under two-worker topology; jobs Map unpruned (`rates-backfill.ts:209-212`, `best-rate-ratchet-db.ts:27-37`) | Fleet advisory lock; conditional-UPDATE ratchet | M | no |
| R-6 | 🟡 | `/rates/cached` endpoints serve pre-markup money; dormant FE markup engine still exported (`routes/rates.ts:678-753`, `web/src/utils/markups.ts`) | Markups in cached endpoints; delete FE engine | S | no |
| R-7 | 🟡 | UTC day-bucket vs 24h TTL → daily 4–5pm PT cache roll + fingerprint-mismatch churn (`rates.ts:565-567,252-259`) | Bucket in ship-from TZ; slave expiry to min(TTL, roll) | M | no |
| R-8 | 🟡 | `rate_cache` no eviction — new rows daily forever (`schema/rates.ts:13-26`) | Nightly delete >3 days + fetchedAt index | S | no |
| R-9 | 🟡 | No PO Box handling — Best Rate can pick undeliverable UPS/FedEx | PO Box axis in address classification + eligibility | M | no |
| R-10 | ⚪ | Fingerprint `st=` collision (state vs storeId); dead tier helpers; duplicated limiter constants (`rate-fingerprint.ts:183,188`, `rates-backfill.ts:59-90`) | `sid=` + version bump; delete dead code | S | no |

### Syncing (SY)

| ID | Sev | Finding | Fix | Effort | Lock |
|----|-----|---------|-----|--------|------|
| SY-1 | 🔴 | Full resync rewinds shipment watermark to oldest walked page; cadence crawls history for days (`shipment-sync.ts:564-568,680-681`) | `GREATEST(stored,new)` guard; separate backfill cursor | S | no |
| SY-2 | 🔴 | Walmart confirm: no "already shipped" idempotent branch; thrown 4xx always retryable → duplicate updates / false confirmation_failed (`walmart.ts:322-323`, `outbox.ts:1063-1123`) | Map duplicate→ok (mirror eBay 409); 4xx terminal | M | yes |
| SY-3 | 🔴 | Deadline abandons handlers that keep writing after lane lock release; no UNIQUE on `shipments.label_shipment_id` → duplicate rows + double package consumption (`sync-job-queue.ts:804-822`, `schema/shipments.ts:76-88`) | Thread AbortSignal; partial UNIQUE + ON CONFLICT | M | yes |
| SY-4 | 🔴 | Watchdog escalates to Render redeploys it can't verify; 3 concurrent drivers race non-atomic cooldown → churn loop (`shipment-sync-watchdog.ts:660-755`) | Gate on worker heartbeat; per-account alerts; advisory-lock the tick | M | no |
| SY-5 | 🟡 | Re-sync UPDATE nulls order_id/client_id, rewrites source on lookup miss — shipped shipments unlink from billing (`shipment-sync.ts:334-350`) | COALESCE-preserve on update branch | S | yes |
| SY-6 | 🟡 | `externally_shipped` one-way latch set by our own confirmations echoing back — blocks re-labels after voids (`shopify.ts:1420`, `store-order-import.ts:490`) | Don't latch when non-voided PrepShip shipment exists | M | no |
| SY-7 | 🟡 | Awaiting-order import has no resume cursor — wide backlogs re-read same pages; new orders starve (`order-sync.ts:930-956`) | Persist awaiting-page cursor | M | no |
| SY-8 | 🟡 | Shopify sync: unbounded loop, non-monotonic cursor, mid-walk persistence (`shopify-order-sync.ts:136-146,199-234`) | GREATEST in SQL + budget + signal checkpoints | S–M | no |
| SY-9 | 🟡 | Watermark keys derived from client *name* — rename resets sync windows (`order-sync.ts:566-622`) | Key by account id, legacy fallback | S | no |
| SY-10 | ⚪ | Resume cursor uses server-TZ `Date.parse` on PT wall-clock (`shipment-sync.ts:612-615`); webhook fallback dedupe buckets by 5-min wall window | `parseShipStationV1Date`; bucket by occurredAt | S | no |

### Billing (B)

| ID | Sev | Finding | Fix | Effort | Lock |
|----|-----|---------|-----|--------|------|
| B-1 | 🔴 | Return labels contaminate generation (no `isReturn` filter) → double-billed or non-deterministic shipping (`billing.ts:890-894,2325`) | `isReturn=false` in generator + details; multi-shipment policy | S/M | no |
| B-2 | 🔴 | Finalization inert: nothing sets `invoiced=true`; sent invoices silently re-priced by later edits (`billing-finalization-policy.ts:47-336`, `billing.ts:426-450`) | Ship `POST /billing/finalize` + credit-note path | M | no |
| B-3 | 🔴 | Label void emits no billing signal — clients stay billed for voided postage; status says "up to date" (`labels.ts:2843-2855`, `billing.ts:406-668`) | Mark billing dirty + clear status cache in void path | S–M | yes |
| B-4 | 🟡 | Storage lines: NULL orderId defeats unique constraint; no generation mutex → concurrent generates double-bill storage (`schema/billing.ts:69-73`, `billing.ts:1790-1839`) | Partial unique index + advisory lock | S | no |
| B-5 | 🟡 | Cross-period ship-date moves strand/drop lines (description-keyed idempotency + window-scoped delete + DO NOTHING) (`billing.ts:1551-1576`) | Order-scoped delete or DO UPDATE; log conflicts | M | no |
| B-6 | 🟡 | Cross-tenant writes bypass scope: `PUT /config/:clientId`, `set-default` box prices (`routes/billing.ts:308-363,2138-2161`) | Scope predicates (pattern exists) | S | no |
| B-7 | 🟡 | Pricing/config mutations unaudited; box-resolution history overwritten in place (`routes/billing.ts:308-363,952-993`) | recordAuditEvent with before/after | S | no |
| B-8 | 🟡 | Float money math, three inconsistent rounding helpers (`billing.ts:170-174`, `markup-resolver.ts:22-24`) | One `roundMoney` + boundary test; cents later | S/L | no |
| B-9 | ⚪ | /generate counters inflated; ref-rate backfill appends dupes; export fallback omits box cost (`billing.ts:1525-1576`, `routes/billing.ts:2226-2243,1783-1785`) | returning() counts; unique key upsert; include package_cost | S | no |

### Frontend (FE)

| ID | Sev | Finding | Fix | Effort | Lock |
|----|-----|---------|-----|--------|------|
| FE-1 | 🔴 | Monolithic views re-render every row on any tick: OrdersView 7,016 lines/69 useState/0 memo; OrderRow memo defeated by per-render `renderTableCell` (`OrdersView.tsx:5944`, `OrderRow.tsx:1-16`) | Stabilize renderTableCell; continue PS-317 extraction | M–L | adjacent |
| FE-2 | 🔴 | View switches remount + refetch everything (`AnimatePresence mode="wait"` + useEffect fetching; Dashboard ~10 endpoints/visit) (`Home.tsx:1226-1228`, `DashboardView.tsx:1731-1856`) | Migrate view GETs to useQuery per view | M | no |
| FE-3 | 🔴 | Entire 3,191-line apiClient in eager entry chunk via one context import (`contexts/MarkupsContext.tsx:12`) | Direct api.get/put + dynamic import | S | no |
| FE-4 | 🔴 | Polling fleet: 10 timers; 2.5s recalc refetch + 2.7-min settle burst; two ungated (`OrdersView.tsx:545-617`) | Visibility gates; backoff; shared refetchInterval queries | S–M | adjacent |
| FE-5 | 🟡 | recharts (430kB, largest chunk) statically imported by AnalysisView (`AnalysisView.tsx:14` vs `DashboardView.tsx:71` done right) | Lazy AnalysisCharts | S | no |
| FE-6 | 🟡 | No virtualization; 200-row cap is the only defense (`OrdersView.tsx:660-684`) | TanStack Virtual on Orders/Inventory | M | adjacent |
| FE-7 | 🟡 | Bulk-selection hydration: up to 25 sequential pages of full order JSON (`v2-apiClient.ts:820-830`) | Parallelize now; backend bulk-snapshot endpoint later | S/M | no |
| FE-8 | 🟡 | Three parallel caches for same data (two TanStack keys + cachedReads) (`useClients.ts:36-40`, `v2-apiClient/shared.ts:473-568`) | One TanStack key per endpoint | M | no |
| FE-9 | ⚪ | framer-motion in shell + mode="wait" tax (~220–440ms/nav); two icon libs; 76kB inline-SVG carrier logos; no JS budget guard | CSS transitions; consolidate; static .svg; extend guard | S–M | no |

### Platform (PL)

| ID | Sev | Finding | Fix | Effort | Lock |
|----|-----|---------|-----|--------|------|
| PL-1 | 🔴 | Inventory deductions fire-and-forget (warn-only, never retried) — ledger silently diverges; idempotency guard already exists (`labels.ts:1358,2028,2309`, `shipment-sync.ts:704-711`) | Outbox-lane deductions | M | yes |
| PL-2 | 🔴 | Runtime DDL in 31 files; `ALTER TABLE shipments` on first label purchase after every deploy (`db/ensure-shipments-selected-rate-cost.ts:14`, `labels.ts:2259-2262`) | Boot-time ensure gate; fold stable tables into Drizzle | M | part |
| PL-3 | 🔴 | `routes/orders.ts` (4,937 lines, 28 routes) owns canonical order model + commands — violates PS-316 (`orders.ts:693-1043,4011-4403`) | Extract read-model + command services around locked helpers | L | yes |
| PL-4 | 🔴 | 2,539 lines live `@ts-nocheck` on credential/OAuth/import boundaries, each opening own DB connection per request (`lib/imported-handlers/*`) | Re-type incrementally (credentials first); shared pool | L | no |
| PL-5 | 🟡 | In-memory job state (26 Maps/21 files); merge/backfill snapshots + final PDFs persisted fire-and-forget after done (`print-queue.ts:2369-2389`, `rates-backfill.ts:209-212`) | Await terminal persists; prune Maps; durable lanes | M | part |
| PL-6 | 🟡 | Force-override rate limiter in-memory — security throttle resets on restart (`lib/force-override-rate-limit.ts:54`) | Back with rate_limiter_state or audit-row count | S | adjacent |
| PL-7 | 🟡 | TOCTOU on `assertOrderEditable` — PATCH racing purchase lands overrides on shipping order (`orders.ts:304-340,4011-4036`) | Carry guard into the write (predicate/transaction) | S–M | yes |
| PL-8 | 🟡 | `INVENTORY_AUTO_DEDUCT` fails open on typos, bypasses validated env (`fulfillment-deductions.ts:135-139`) | env.ts booleanFlag + boot log | S | yes |
| PL-9 | 🟡 | Missing indexes `shipments.label_shipment_id`/`tracking_number` (=M2); no UNIQUE on label identity; pool sprawl (14 conns/process + per-request clients) (`schema/shipments.ts:76-88`) | CONCURRENTLY indexes + partial UNIQUE; shared pool | S/M | yes |
| PL-10 | 🟡 | Five parallel job/queue mechanisms; interval scheduler can double-run API+worker; 107 bare catch blocks, no error counter (`sync-scheduler.ts:460-641`) | Finish pg-boss consolidation; shared reportError sink | L/M | no |
| PL-11 | ⚪ | No SIGTERM drain on API; /ready = /deep runs count(*) over print history per probe; console-only logging, requestId never propagates (`main.ts`, `routes/health.ts:84-158`) | Graceful shutdown; split ready/deep; structured log helper | S–M | no |

---

## THE MASTER CHECKLIST

> **Week-1 execution log (2026-07-13, override `unlock shipped data` typed by DJ):**
> Items 1.1–1.14 implemented and verified the same day (typecheck + PS-313 + PS-050 +
> PS-403 ×2 + PS-350 + PS-412 all green). Prod migration
> `audit_2026_07_13_week1_indexes_and_rls` applied (2 shipments indexes, storage
> unique index, RLS ×6). Notes: B-3 landed **better than planned** — void staleness is
> derived from source rows inside the billing status probe, so no locked-file edit was
> needed and historical unreversed voids surface too. 1.21's UNIQUE index is **blocked
> on one pre-existing duplicate pair** (`label_shipment_id` 155168257, 2 rows) — needs
> DJ's decision (resolving it mutates shipped rows = separate confirmation). The M3
> pg_stat offender (2.5–3.4s queries) turned out to be dead code from a pre-b6c31fb1
> deploy (call count frozen); the live watchdog descendant was rewritten anyway.

Verification bundle for every batch: `npm run typecheck` · `npm run test:rate-source-of-truth` (mandatory for anything touching rates/proof) · relevant guards (`ps-050`, `ps-241`, `ps-350`, `ps-403`, `ps-412`) · for **[LOCK]** items: `unlock shipped data` override + comment + commit annotation per CLAUDE.md.

### Phase 1 — Money & correctness (~2 weeks)

Week 1, no lockdown needed:

- [x] 1.1 Origin in rate fingerprint + `RATE_CACHE_VERSION` bump (**C4**) — `sf=`/`sfc=` parts, explicit-origin only (default flows byte-identical → no proof churn); version → ground-saver-v3 purges pre-origin rows
- [x] 1.2 Billing `isReturn=false` filter in generator + details (**B-1**)
- [x] 1.3 Monotonic watermark guards: shipment sync + Shopify cursor (**SY-1**, **SY-8** part) — `Math.max(stored, candidate)` + SQL `GREATEST`; zero-page runs stand still
- [x] 1.4 Storage-line partial unique index + advisory lock on generate (**B-4**) — `pg_advisory_xact_lock` inside the storage tx + `billing_li_storage_unique_idx` (0 violations existed)
- [x] 1.5 Billing scope checks on `PUT /config/:clientId` + `set-default` (**B-6**)
- [x] 1.6 Index `shipments(tracking_number)` + skip-when-bound loop fix (**M2**, **PL-9** part) — enrichment now does 1 indexed SELECT/page instead of ~500 blind UPDATEs
- [x] 1.7 Watchdog query rewrite: two NOT EXISTS (**M3**) — live descendant fixed; pg_stat offender confirmed dead code (frozen call count)
- [x] 1.8 RLS on the 6 PostgREST-exposed tables (**M5**) — verified FE has zero PostgREST table reads first
- [x] 1.9 Pool `max_lifetime` (15 min, env `DB_MAX_LIFETIME_SECONDS`) on main/advisory/health pools + `dbWrite` probe in deep health

Week 1, needs `unlock shipped data`:

- [x] 1.10 **[LOCK]** Purchase POST single-attempt on 5xx (**C1**) — `retryOn5xx: false` on `ssCreateLabel` + `ssCreateReturnLabel`; 429 retries kept (never processed → safe)
- [x] 1.11 **[LOCK]** Purchase-lock TTL 45 min > pg-boss 30-min retry horizon (**C2** interim; env-overridable)
- [x] 1.12 **[LOCK]** Source filter on missing-confirmation sweep (**C3**) — fail-closed allowlist `prepship, prepship_v2, shipp, walmart_shipping, test_offline` (audit's suggested list was missing the direct carriers; verified against prod source values)
- [x] 1.13 Void → billing staleness (**B-3**) — implemented as a 4th staleness input (voided-after-billed detection from source rows) in the billing owner; **no locked-file edit needed**, catches historical voids retroactively
- [x] 1.14 **[LOCK]** Shipment UPDATE preserves orderId/clientId/source (**SY-5**)

Week 2:

- [x] 1.15 **[LOCK/PS-021]** Walmart confirm idempotent branch + terminal-4xx classification (**SY-2**) — already-shipped/duplicate settles as success; non-transient 4xx terminal
- [x] 1.16 **[LOCK]** Print-queue transient-vs-terminal error classification (**PQ-5**) — circuit-open/429 now `failed_retryable` (`provider_unavailable`/`provider_rate_limited`); 5xx/timeout stay terminal (unknown outcome); `CircuitBreakerOpenError` added for structural checks
- [x] 1.17 Force-override limiter → durable (**PL-6**) — derived from append-only `lockdown_override` audit rows; in-memory fallback; PS-231 guard extended (16 ok)
- [x] 1.18 **[LOCK]** `INVENTORY_AUTO_DEDUCT` → validated env + first-use log (**PL-8**) — typo now fails toward kill-switch purpose
- [x] 1.19 Audit events on billing pricing/config/box-resolution writes (**B-7**) — config upsert, package prices, set-default (all touched clientIds), ref-rate backfills, box-resolution before/after
- [x] 1.20 **[LOCK]** Durable purchase-intent record — the complete C2 fix (**C2**) — `label_purchase_intents` table (prod migration `audit_2026_07_13_label_purchase_intents`); intent written before provider call, resolved by classification on buy error, `reconcile_required` on persist-after-buy failure, and every new purchase fails closed (`LABEL_PURCHASE_RECONCILE_REQUIRED`) while any unresolved intent exists — no automatic re-buy at any retry horizon
- [x] 1.21 **[LOCK]** Partial UNIQUE on `shipments(label_shipment_id)` + ON CONFLICT in upsert (**SY-3** part) — DJ approved deleting duplicate row id 24462 (kept original 24356, repointed its 3 billing lines first; order 969778 had 1 shipping line — no double-bill occurred). Migration `audit_2026_07_13_dedupe_label_shipment_unique`: dedupe + UNIQUE index + dropped redundant plain index. Sync insert now `onConflictDoNothing` on the unique target, and skipped rows skip package consumption (the winner already consumed). **Phase 1 = 21/21 complete.**

### Phase 2 — Performance, measured wins (weeks 2–4)

- [x] 2.1 FE poller hygiene: visibility gates on both OrdersView pollers + 60s idle backoff on backfill-attach (**FE-4**, shrinks **M1**; settle-burst found to be dead code — spun off as its own task chip) (2026-07-13)
- [x] 2.2 **View GETs → TanStack Query complete** (2026-07-14): Dashboard (13 requests → 9 stable-keyed queries; staged critical-then-idle first paint; cached revisit = zero requests), Billing (6 GETs; mutation invalidation preserves prior refresh sets), Analysis (4 GETs; table critical/chart idle-gated/drawer keyed by SKU+range), and Settings (13 mount-time GETs across the shell and section views; one stable key per endpoint; active panels idle-armed; cached data paints before re-enable). `mode="wait"` navigation gate removed. Settings focused query guard, affected endpoint-literal assertions, typecheck, and production build green; unrelated pre-existing PS-239 route-allowlist, PS-106 browse-regex, and Automation copy guard failures remain separately visible.
- [x] 2.3 MarkupsContext import break (**FE-3**) — entry chunk 135→70 kB (gz 38→21, −45%) (2026-07-13)
- [x] 2.4 Lazy recharts in AnalysisView (**FE-5**) — 430 kB charts chunk now lazy-only (2026-07-13)
- [x] 2.5 Stabilize `renderTableCell` + row-opener identities with cellStateEpoch memo contract — OrderRow memo now bails on poll ticks/hover/unrelated state (**FE-1** first slice; render-parity guards green) (2026-07-13)
- [x] 2.6 rate_cache + direct-carrier cache eviction tick (**R-8**) — 3-day DELETE riding the 5-min reap cadence; no index needed at current row counts (2026-07-13)
- [x] 2.7 Day-bucket → ship-from TZ (**R-7**) — bucket now rolls at America/Los_Angeles midnight instead of 4–5pm PT mid-shift; expiry-slaving refinement deferred (2026-07-13)
- [x] 2.8 **Rate infrastructure tail complete** (2026-07-14): ShipStation v2 admission now has one canonical gate in `ssRequest` for labels/sync/discovery/rating; the duplicate `rates.ts` timestamp budget is deleted while its interactive-first concurrency queue remains. Per-key durable total + background-reserve buckets are wired behind the existing `RATE_LIMITER_BACKEND=durable` switch (fleet-wide burst and per-minute headroom), and limiter waits/retry backoffs honor cancellation. Direct-carrier deadlines now propagate `AbortSignal` through token/discovery/rate/retry work for EasyPost, UPS, FedEx, USPS, ShipEngine, eBay Shipping, Amazon Shipping, Walmart Shipping, and Shipp. The timeout owner preserves carrier-specific timeout diagnostics while aborting the losing work. Guards green: PS-050 exactness/limiter, PS-256 durable limiter, PS-403 provider timeouts, priority behavior, PS-241 fanout, batched replay, connector registry/architecture, PS-313 rate source-of-truth, typecheck, and production build. No frontend or shipped/cancelled path changed. Sync-handler signal checkpoints previously landed in 75ad69dc (SY-3 signal part + SY-8 Shopify page budget).
- [x] 2.9 **Print queue write-load and memory tail complete** (2026-07-13, `8eb77e4b`): counters-only progress updates + one-statement batched item upserts with the duplicate start write removed (**PQ-7**), worker job/payload map eviction in `finally` after every run (**PQ-6**), and a 500-entry `/print` request cap matching the existing clear limit (**PQ-8**). Original verification: typecheck, PS-403 print-queue worker recovery + PDF chunks, and PS-350.
- [x] 2.10 **ShipStation batched rate fanout enabled and production-proven** (2026-07-14): implementation landed via `ee2f9c8e`; spanning limiter pins (PS-050), per-key scoping + dedupe identity, 8s abortable batch-probe cap, and the fail-closed `scripts/probe-batched-rate-estimate.ts` are in place. The all-source live probe returned exact attributed batch-vs-single rate multisets for all 14 primary/KFG carrier comparisons. `SHIPSTATION_BATCHED_RATE_FANOUT=true` was then enabled on Render `prepshipv4-api` and deploy `dep-d9aov1daeets739mmgb0` reached live. One authenticated cache-first Recalculate All (`18dbdb41-7e37-4faa-8d78-5570c50b9ed0`) processed 20 awaiting orders: 17 updated, 3 safely skipped for missing real dimensions, 0 failed. Render recorded one successful `/v2/rates/estimate` batch for the observed seven-carrier ShipStation fanout, with no single-account fallback, rejected batch rows, retries, or app errors; `/health/ready` remained 200. No labels/postage, marketplace notifications, or shipped/cancelled mutations occurred.
- [x] 2.11 Split `/health/ready` (db + dbWrite + eventLoop only) from `/deep` (full diagnostics incl. table aggregates) (**PL-11** part, 2026-07-13)
- [x] 2.12 Unindexed-FK indexes (10) + PK `(order_id, line_type)` on `billing_manual_overrides` — migration `audit_2026_07_13_fk_indexes_and_pk`; unused-index drops deferred to Phase 4 pending a re-check of pg_stat usage (**M6**, 2026-07-13)
- [x] 2.13 Backfill money-basis fix: finalize from MARKED cheapest (mirror browse), raw-restore only the persisted copy — kills spurious "Rate changed or expired" on marked carriers (**R-1**, 2026-07-13)
- [x] 2.14 Markups before re-pick in cache repair + sanitizer (**R-2**); read-time markups + backend-owned display re-pick (`markRateCacheRowForDisplay` in the rate owner, PS-313-guard-verified) on `/rates/cached` + `/cached/bulk` (**R-6**, 2026-07-13; FE markup-engine deletion running in FE workstream)

### Phase 3 — Architecture (month 2)

- [x] 3.1 **[LOCK] Orders read-model + override/rate command extraction complete** (2026-07-14, user override `unlock shipped data`): canonical DTO/detail assembly moved to `services/orders-read-model.ts`; box/dims coherence, override upsert, atomic Apply Best Rate, and saved Best Rate commands moved to `services/orders-overrides-command.ts`. `routes/orders.ts` fell from 4,937 to 4,295 lines and retains `LOCKED_STATUSES`, `assertOrderEditable`, caller scope/auth, and guard-before-delegation ordering. `test:audit-orders-service-boundary` is mandatory in the SOT guard pack; PL-3, PS-302/322/333-345, PS-184/207/276/292/304/411, API-contract, lockdown, strict typecheck, and production build proofs pass. No provider calls, labels/postage, marketplace notifications, or production shipped/cancelled mutations occurred. (**PL-3**)
- [x] 3.2 **[LOCK] Durable pg-boss + inventory-deduction outbox complete** (2026-07-14, user override `unlock shipped data`): API/worker scheduler duplication and `USE_PG_BOSS_SCHEDULER` were deleted; recurring sync, maintenance, and carrier-snapshot work now uses Postgres-backed `pg-boss.schedule` singleton cadence, while the independent heartbeat/watchdog remains process-local. Shipping/status callers enqueue `inventory_deduction_requested`; the fulfillment outbox retries it, repairs recent shipped rows missed across the commit/enqueue crash window, and delegates stock math, `INVENTORY_AUTO_DEDUCT`, and ledger idempotency unchanged to `fulfillment-deductions.ts`. Inventory events cannot mutate marketplace-confirmation or order lifecycle state. `test:audit-pg-boss-inventory-outbox`, strict typecheck, production build, scheduler/reaper/watchdog, shipment recovery, connector, lockdown, and inventory guards pass. No labels/postage, marketplace notifications, or production shipped/cancelled mutations occurred. (**PL-10**, **PL-1**)
- [x] 3.3 **[LOCK-part] Runtime DDL elimination complete** (2026-07-14, user override `unlock shipped data`): all 31 production runtime schema writers now delegate to the single memoized `services/runtime-schema-readiness.ts` gate; API verifies it before listening and worker verifies it before starting jobs. Existing stable objects are owned by migrations `0019`–`0061`; new additive migration `0062_runtime_schema_ownership.sql` owns the remaining rate cache/limiter, billing waiver/override, label lock/intent, print-queue job/PDF, Rate Browser job, and worker-status sidecars. The production DDL guard now rejects schema DDL under `src`/`api` and is mandatory in the SOT guard pack. Migration-first deployment is required. Strict typecheck, production build, runtime-DDL/SOT/rate, affected historical, finalized-billing, lockdown, inventory, and master-quick proofs pass. No migration was applied, no provider calls or labels/postage occurred, and no production shipped/cancelled data was mutated. (**PL-2**)
- [x] 3.4 **Imported-handler typing + shared DB pool complete** (2026-07-14, user override `unlock shipped data`): all eight `src/lib/imported-handlers` modules now compile under strict TypeScript without `@ts-nocheck`; Node request/response and JSON compatibility are owned by `lib/node-handler.ts`, while credential normalization, persistence, and readiness remain in their existing canonical owners. Normal credential, OAuth, marketplace import, carrier verification, Walmart fee, and maintenance paths now use the process-wide `db/client.ts` pool; only documented health/advisory-lock isolation boundaries retain dedicated connections. The legacy store-credential cutover was removed from request handling and moved to idempotent migration `0063_credential_account_cutover.sql`, so migration-first deployment is required. `test:audit-imported-handler-boundary` is mandatory in the SOT guard pack; strict typecheck, production build, credential, marketplace, connector-orchestrator, runtime-DDL, PS-313, lockdown, and upstream shipping-safety proofs pass. No migration was applied, no live credential/provider call, label/postage purchase, marketplace notification, or production shipped/cancelled mutation occurred. (**PL-4**)
- [x] 3.5 **[LOCK-part] Merge durability production canary complete and enabled** (2026-07-14, user override `unlock shipped data`): migration `0064_print_queue_merge_jobs.sql` is applied and monotonic per-jobId snapshots, awaited initial/chunk/terminal persistence, and missing-worker stale derivation remain owned by the backend print-queue boundary. Flag-OFF concurrent mock-only jobs `81bf573d-340d-4457-879d-0bd264c1de01` and `a0304221-64d4-4bd7-9bd8-6780af5a9c25` both completed with isolated two-entry snapshots and zero durable artifact rows. After DJ's runtime go/no-go, `DURABLE_PRINT_QUEUE_PDF=true` was deployed to the Render API in `dep-d9as4be7r5hc739cadk0`; mock-only job `284df37c-9292-43fe-ae11-83bc89f2ed65` stored one job, PDF, and chunk row. A fresh-process restart deploy (`dep-d9as5peq1p3s73d95fb0`) rehydrated status `done` and served the identical 3,329-byte chunk (`SHA-256 a5c6c7e244c4402d6c1352ae06336f7402dd87476a801a2e784fc5feede40d07`). Synthetic queue fixtures were removed; health/ready/deep remained green; shipment, mock-label, fulfillment-outbox, and shipped/cancelled counts were unchanged. No real label/postage purchase, marketplace notification, or production shipped/cancelled mutation occurred. (**PQ-4**, **PL-5**)
- [x] 3.6 **Billing finalize endpoint + credit-note path complete and deployed** (2026-07-14): `billing-finalization-policy.ts` is the canonical close owner; scoped `financials:write` `POST /billing/finalize` atomically freezes the exact client/period lines, shared invoice subtotal, counts, and authenticated actor, while exact retries are idempotent and overlapping/straddling closes fail closed. Generation now rejects closed periods. Additive migration `0065_billing_close_workflow.sql` owns immutable close records, append-only reasoned credit notes, cross-process period locks, closed-period line guards, durable credit idempotency, and aggregate over-credit rejection; boot readiness advances to 0065. Invoice export and close share `billing-invoice-totals.ts`; scoped read endpoints and `POST /billing/credit-notes` keep routes thin and the future Phase 5.7 UI display/action-only. The PS-336 placement record is in `docs/ps-tickets/ps-412-billing-close-workflow.md`; the expanded PGlite PS-412 guard is mandatory in the SOT pack. Strict typecheck, production build, PS-412, PS-416, PS-323, PS-313, runtime-DDL, SOT-pack, and lockdown proofs pass. Migration 0065 was applied transactionally before deploy and verified at 4 functions, 7 triggers, and 2 RLS tables with protected order/shipment counts unchanged. Commit `89a31558` is live on API deploy `dep-d9astn67r5hc739dh7rg`, main worker `dep-d9asu9mrnols73db7ri0`, and print worker `dep-d9assnlckfvc73btog4g`; `/health`, `/health/ready`, and `/health/deep` returned 200, and both new close tables remained empty. No production billing finalization/credit, label/postage, marketplace notification, or shipped/cancelled data mutation occurred. (**B-2**)
- [x] 3.7 **Canonical money rounding complete** (2026-07-14): `src/lib/money.ts#roundMoney` now owns backend numeric dollar-to-cent conversion with symmetric half-away-from-zero ties and scale-aware binary-float tolerance. Billing generation, summary/invoice totals, storage, manual/HUGRAB/box/selected-rate decisions, canonical markup, and backend rate-money DTOs delegate at their cent boundaries; local scoped `round2`/`roundCents` owners are removed. The canonical `$12.50 × 15%` quote/invoice result is now `$14.38` instead of float-drifted `$14.37`, with negative-credit parity pinned too. The PS-336 placement record is `docs/ps-tickets/audit-3.7-money-rounding.md`; `test:audit-money-rounding` is mandatory in the SOT pack. Half-cent, PS-798, markup, billing/storage/box/selected-rate/HUGRAB, PS-177/207/220/323/373/381/392/398/412/416, PS-313, strict typecheck, production build, SOT-pack, and lockdown proofs pass. Integer-cent persistence remains deferred per B-8. No DB migration, provider call, label/postage, marketplace notification, frontend authority, or production shipped/cancelled mutation occurred. (**B-8**)
- [x] 3.8 **Shared structured money-path logging complete** (2026-07-14, user override `unlock shipped data`): `lib/structured-log.ts` now owns one JSON log shape, Error normalization, identity deduplication, and `AsyncLocalStorage` propagation from the API request ID into awaited and detached work. Billing, Rates/Rate Browser, Shopify rates, label purchase, the global Hono boundary, process failures, and the legacy safe-error adapter delegate to that sink; order-scoped paths add `orderId`, expected business rejections remain structured warnings, and unexpected failures emit once with request context. The PS-336 placement record is `docs/ps-tickets/audit-3.8-structured-money-logging.md`; `test:audit-structured-money-logging` is mandatory in the SOT pack and proves async context, canonical-field protection, JSON shape, and deduplication. Strict typecheck, production build, full SOT pack, PS-313, PS-403, PS-406, PS-202, PS-267, PS-323/412/416, and lockdown proofs pass. The only locked file touched was `src/routes/labels.ts`, for logging/context around existing purchase calls; label admission, proof, purchase, persistence, and shipped/cancelled protections are unchanged. No DB migration, provider call, real label/postage, marketplace notification, frontend authority, or production shipped/cancelled mutation occurred. (**PL-11** part)
- [x] 3.9 **Backend bulk-action snapshot complete** (2026-07-14, user override `unlock shipped data`): scoped `POST /orders/bulk-snapshot` accepts up to the existing 5,000-ID selection ceiling and reuses the canonical Orders list/read-model pipeline, preserving tenant/client/store, assignee, visibility, financial-redaction, lifecycle, package, rate, and shipment DTO facts. `orders-read-model.ts` owns ID normalization, requested-order response ordering, and explicit missing-ID reporting. Selected actions now hydrate only missing IDs with one POST; all-matching actions use one IDs-only read plus one snapshot POST, deleting the prior sequence of up to 25 full-order page requests. The frontend remains transport/interaction-only and every batch mutation retains its backend final guard. The PS-336 placement record is `docs/ps-tickets/audit-3.9-orders-bulk-snapshot.md`; `test:audit-orders-bulk-snapshot` is mandatory in the SOT pack. Strict typecheck, production build, full SOT pack, Orders read-model/API/query/startup/select-all/global-search, PS-304/305/317/320/329, and lockdown proofs pass. Protected files touched were `src/routes/orders.ts` and `web/src/components/Views/OrdersView.tsx`, for read-only snapshot delegation/hydration only; `LOCKED_STATUSES`, `assertOrderEditable`, `isReadOnly`, shipped/cancelled selection gates, and batch suppression are unchanged. No DB migration/write, provider call, label/postage, marketplace notification, or production shipped/cancelled mutation occurred. (**FE-7**)
- [x] 3.10 **[LOCK] Transaction-time order edit lockdown complete** (2026-07-14, user override `unlock shipped data`): `order-editable-write.ts` now owns the final row-locked transaction boundary, re-reading raw/canonical/external lifecycle facts through `resolveOrderLifecycleStatus` immediately before persistence. `assertOrderEditable` remains the early scope/auth/audit guard and carries fail-closed authorization; only its audited admin `?force=1` branch can authorize terminal writes. PATCH, override/rate commands, save-dims, combo-default save/propagation/materialization, and shipped-external source persistence delegate to the final owner; manual external-shipment UPDATEs carry the effective-lifecycle predicate and losing transitions no longer enqueue inventory work or notify marketplaces. Direct guarded writes were removed from the route. The PS-336 placement record is `docs/ps-tickets/audit-3.10-order-editable-write-guard.md`; `test:audit-order-editable-write` is mandatory in the SOT pack. Strict typecheck, production build, full SOT pack, PL-3, PS-411, combo-default, PS-302, PS-192, PS-313, and lockdown proofs pass. Protected files touched were `src/routes/orders.ts`, `src/services/orders-overrides-command.ts`, `src/services/combo-package-defaults.ts`, and `src/services/fulfillment/mark-shipped-externally.ts`; lifecycle locks were strengthened and the admin override remained audited. No DB migration/write, provider call, real label/postage, inventory deduction, marketplace notification, or production shipped/cancelled mutation occurred. (**PL-7**)
- [x] 3.11 **Print-queue lifecycle complete** (2026-07-14, user override `unlock shipped data`): the durable queue-send store now owns atomic cross-process recovery claims with `FOR UPDATE SKIP LOCKED`, durable attempt increments, and visible interruption after a three-attempt cap. The dedicated worker runs the same recovery pass at boot and every 60 seconds; enqueue and continuation payloads are capped at 100 orders, with each successful chunk reloading durable results before scheduling the next. The execution owner cooperatively stops new order admission at the parent deadline and single-flights same-process retries by parent job id; in-flight provider work settles through the existing idempotent purchase boundary. The old non-claiming recovery reader and non-cancelling concurrency pool were removed. The PS-336 placement record is `docs/ps-tickets/audit-3.11-print-queue-lifecycle.md`; `test:audit-print-queue-lifecycle` is mandatory in the SOT pack and behaviorally proves 1,000-order chunking, cancellation, re-entry protection, and explicit PostgreSQL text typing for recovery messages embedded in JSONB (closing the production worker-start `$2` regression). Strict typecheck, production build, full SOT pack, PS-403/400/351/420, worker offload/in-progress recovery, PS-303/269, and lockdown proofs pass. The only protected file touched was `src/services/print-queue.ts`, to delegate queue-send execution control; duplicate-postage checks, selected-rate proof, label purchase ownership, shipped/cancelled edit locks, and provider boundaries were not weakened. No DB migration/write, provider call, real label/postage, marketplace notification, inventory change, or production shipped/cancelled mutation occurred. (**PQ-1**, **PQ-2**, **PQ-3**)
- [x] 3.12 **Sync watchdog lifecycle complete** (2026-07-14, user override `unlock shipped data`): the canonical watchdog tick now serializes the process timer and cron GET/POST recovery drivers with one transaction advisory lock, while `/sync/status` remains side-effect free. Render escalation is heartbeat-gated at both recommendation and control-plane boundaries; fresh workers alert instead of redeploying. Canonical order-sync account diagnostics drive independently attributable account alerts and the durable watchdog snapshot. Awaiting ShipStation account/store passes persist a backend settings cursor, probe newest-first page 1, resume older backlog, retain the cursor on failed/not-started work, and reset only after a complete pass. Store-order import remains the persistence authority for Shopify's translated fulfilled fact and refuses/clears the `externally_shipped` latch when a linked non-voided outbound shipment proves the confirmation was PrepShip-owned. The PS-336 placement record is `docs/ps-tickets/audit-3.12-sync-watchdog-lifecycle.md`; `test:audit-sync-watchdog-lifecycle` is mandatory in the SOT pack. Strict typecheck, production build, full SOT pack, PS-361/397/409, PS-265 run-budget/staleness, advisory-lock, connector/import, Shopify, marketplace reconciliation, and lockdown proofs pass. The only protected logic touched was `src/services/store-order-import.ts`, for a read-only active-shipment reconciliation inside the existing import upsert; terminal-status preservation and shipment history are unchanged. No DB migration/write, provider call, real label/postage, marketplace notification, inventory change, or production shipped/cancelled mutation occurred. (**SY-4**, **SY-6**, **SY-7**)
- [x] 3.13 **Cross-period billing reconciliation complete** (2026-07-14, user override `unlock shipped data`): `generateLineItems` now rebuilds editable lines for current candidate order IDs across all periods while retaining the requested-window stale sweep, so canonical billing-date corrections move lines instead of stranding or silently dropping them. Finalized order/period assertions, editable predicates, and database triggers remain unchanged. Batched inserts use `RETURNING`, so generated counts and totals reflect persisted rows, and any remaining unique conflicts emit a count-only structured warning. The PS-336 placement record is `docs/ps-tickets/audit-3.13-billing-cross-period-reconciliation.md`; `test:audit-billing-cross-period-reconciliation` is mandatory in the SOT pack and behaviorally proves a June-to-July move, stale-window cleanup, unrelated-period preservation, finalized-history preservation, and observable conflict skips. Strict typecheck, production build, full SOT pack, PS-412 finalized billing, billing formula/scope/read-parity/invoice/lockdown guards, and focused billing proofs pass. The only protected file touched was `src/services/billing.ts`, to reconcile derived `billing_line_items`; no orders or shipments were written, finalized history and shipped/cancelled protections were not weakened, and no configured DB write, provider call, real label/postage, inventory change, marketplace notification, or production shipped/cancelled mutation occurred. (**B-5**)

### Phase 4 — Debt cleanup (rolling)

- [x] 4.1 **Dead code and frontend rate-authority cleanup complete** (2026-07-14): the already-orphaned `useSyncPoller.ts` and legacy `utils/markups.ts` money exports removed in `b91e43ca` now have no barrel/tombstone residue; the unreachable `ServiceTier`/`classifyTier`/`pickBestForTier` cluster and its `rateTotal` import are deleted from `rates-backfill.ts`. Unused markup-context math/cache/error APIs are gone. The live Rates page now calls the existing backend `browseRates` DTO path directly and renders `selectedRateCost`, `cShippingRateAmount`, `shippingMarginAmount`, and backend `selectedRateKey`/`bestRate` identity instead of reloading settings, applying frontend markup math, or reducing a local cheapest row. The PS-336 placement record is `docs/ps-tickets/audit-4.1-dead-code-cleanup.md`; `test:audit-dead-code-cleanup` behaviorally proves backend money/best-rate pass-through and is mandatory in the SOT pack. Strict typecheck, production build, all 22 SOT guards, PS-079/124/177/178/313/320/339, markup single-source, settings scope, and Orders startup guards pass. No shipped/cancelled or other protected path changed; no database/provider call, label/postage purchase, marketplace notification, inventory change, or production mutation occurred. (**R-10**, **FE-9** part)
- [x] 4.2 **Limiter configuration + fingerprint hygiene complete** (2026-07-14): `src/lib/shipstation/rate-limit-config.ts` now owns the shared admission window, the intentionally stricter V1 38/minute budget, and all clamped V2 environment-driven budget/burst/reserve values; both ShipStation transports delegate to it while the V2 client preserves its prior public constant exports. The canonical rate-request fingerprint retains `st=` for destination state and now serializes store identity as `sid=`, and `RATE_CACHE_VERSION` advances to `ground-saver-v4` so pre-hygiene cache/proof identities cannot mix. The PS-336 placement record is `docs/ps-tickets/audit-4.2-limiter-fingerprint-hygiene.md`; `test:audit-limiter-fingerprint-hygiene` executes the collision boundary, pins both transport callers to the config owner, and is mandatory in the SOT pack. Strict typecheck, production build, all 23 SOT guards, PS-050 exactness/admission, PS-256 durable limiter, priority-limiter behavior, PS-085 shipping workflow, PS-277 fingerprint consistency, and PS-313 rate-source-of-truth proofs pass. No shipped/cancelled or other protected path changed; no configured DB write, provider call, label/postage purchase, marketplace notification, inventory change, or production mutation occurred. (**R-10**)
- [x] 4.3 Watermark keys by account id, not client name (**SY-9**); PT-aware cursor parse (**SY-10**); webhook dedupe by occurredAt (**SY-10**)
- [x] 4.4 **Frontend cache + bundle hygiene complete** (2026-07-14): active-only and include-inactive client reads now use one canonical TanStack key per endpoint through the app-wide `QueryClient`; hooks, views, imperative compatibility reads, and mutation invalidation delegate to those keys, and the parallel `cachedReads` client entry plus view-specific keys are gone. Analysis retains its guard-pinned inline GET literal while sharing the canonical key. `lucide-react` is the sole icon library; the six carrier marks moved from TSX into static `/carrier-logos/*.svg` assets; remaining `AnimatePresence mode="wait"` props are removed. The production bundle guard now checks every JavaScript chunk at 500,000 bytes raw / 140,000 bytes gzip; all 59 chunks pass, with a 430,881-byte raw / 117,263-byte gzip maximum. The PS-336 placement record is `docs/ps-tickets/audit-4.4-frontend-cache-bundle-hygiene.md`; `test:audit-frontend-cache-bundle-hygiene` is mandatory in the SOT pack. Strict typecheck, production build/budget, client/Analysis/Settings/Marketplace Fees/Orders startup guards, and all 26 SOT guards pass. No shipped/cancelled or other protected path changed; no database/provider call, label/postage purchase, marketplace notification, inventory change, or production mutation occurred. (**FE-8**, **FE-9**)
- [x] 4.5 **Billing small fixes complete** (2026-07-14, per user override `unlock shipped data`): `generateLineItems` now counts and totals only rows actually persisted by PostgreSQL `RETURNING`; `billing-ref-rate-store.ts` owns normalized reference-rate identity and conflict updates; migration `0066_billing_ref_rate_identity.sql` keeps the newest duplicate and adds a `UNIQUE NULLS NOT DISTINCT` key required by runtime schema readiness; and the shared backend `billing-invoice-row-total.ts` owner preserves positive frozen `row_total` values while including generated package cost in the zero-total compatibility fallback used by HTML, XLSX, and CSV. The PS-336 placement record is `docs/ps-tickets/audit-4.5-billing-small-fixes.md`; `test:audit-billing-small-fixes` behaviorally proves the identity/upsert migration and invoice fallback and is mandatory in the SOT pack. Strict typecheck, production build, all 27 SOT guards, billing generation/ref-rate/export/scope/fail-closed/rounding guards, and shipped/cancelled lockdown guards pass. Shipped-adjacent reads changed only in `src/services/billing.ts`, `src/routes/billing.ts`, `src/routes/billing-invoice-csv.ts`, and `src/services/ref-rates-fetch.ts`; order/shipment source rows remain read-only and no protection was weakened. Migration 0066 was exercised only in ephemeral PGlite and was not applied to a configured database; no provider call, real label/postage purchase, marketplace notification, inventory change, or production shipped/cancelled mutation occurred. (**B-9**)
- [x] 4.6 **API process lifecycle complete** (2026-07-14): `api-process-lifecycle.ts` now owns one idempotent shutdown state machine for the HTTP server retained from `serve`; SIGINT/SIGTERM stop new admission, close idle connections, let active requests drain for a validated 25-second default, and force-close with a non-zero exit if the deadline expires. Both unhandled rejections and uncaught exceptions delegate to the same structured failure counter; the validated default limit of three opens the breaker and drains with exit 1 so the process supervisor restarts poisoned state. `main.ts` is thin event wiring, while startup-readiness failure remains fail-fast. The PS-336 placement record is `docs/ps-tickets/audit-4.6-api-process-lifecycle.md`; `test:audit-api-process-lifecycle` behaviorally proves duplicate-signal idempotency, clean active-request drain, fatal threshold, and forced timeout and is mandatory in the SOT pack. Strict typecheck, production build, all 28 SOT guards, structured logging, runtime readiness, health/deep readiness, API metrics, maintenance startup, shipment watchdog, and pg-boss scheduler proofs pass. No frontend, shipped/cancelled, or other protected path changed; no database/provider call, label/postage purchase, marketplace notification, inventory change, or production mutation occurred. (**PL-11**)
- [x] 4.7 **Print Queue small fixes complete** (2026-07-14, per user override `unlock shipped data`): `addToQueue` now carries queue finality into the PostgreSQL conflict update with `setWhere: status = 'queued'`; a concurrent printed/delivered winner produces no `RETURNING` row, raises the existing structured 409, and cannot run confirmation repair or revive physical-print history. API dispatch lazily reuses one supervised PgBoss enqueue instance instead of starting/stopping a new supervisor per request, while failed initialization resets for a later retry. `durable-status-read.ts` preserves missing-versus-timeout outcomes; `/batch-send/status/:jobId` keeps true/scoped misses at 404, serves in-memory truth when available, and returns retryable `PRINT_QUEUE_STATUS_UNAVAILABLE` 503 when the durable read is slow. The PS-336 placement record is `docs/ps-tickets/audit-4.7-print-queue-small-fixes.md`; `test:audit-print-queue-small-fixes` behaviorally proves the finalized-row race in ephemeral PostgreSQL and the timeout distinction, pins shared enqueue ownership and HTTP mapping, and is mandatory in the SOT pack. Strict typecheck, production build, all 29 SOT guards, PS-053/104/256/269/303/346/351/354/360/400/401/403/420, Print Queue durability/lifecycle/worker/scope/persistence/label/PDF guards, and shipped/cancelled lockdown guards pass. Protected/shipped-adjacent files touched were `src/services/print-queue.ts`, `src/services/print-queue-worker.ts`, and `src/routes/print-queue.ts`; finality and duplicate-postage protections were strengthened, and no label purchase, provider, order, shipment, inventory, or marketplace-confirmation authority moved. No configured database/provider call, real label/postage purchase, marketplace notification, inventory change, or production shipped/cancelled mutation occurred. (**PQ-9**, **PQ-10**)
- [x] 4.8 **Backfill diagnostics complete** (2026-07-15, per user override `unlock shipped data`): `rates-backfill.ts` remains the canonical per-order outcome classifier and now records missing-dimension, no-rate, and no-downgrade outcomes into an independent five-entry `skipSamples` buffer while actual exceptions alone populate the independently capped `failureSamples` buffer. The durable snapshot carries both fields, and legacy snapshots without `skipSamples` normalize to an empty array. The Rates status endpoint and read-only Orders sync-status DTO pass both backend-classified buffers through without route/frontend reclassification. The PS-336 placement record is `docs/ps-tickets/audit-4.8-backfill-diagnostics.md`; `test:audit-backfill-diagnostics` behaviorally proves independent capacity, no cross-contamination, and legacy normalization and is mandatory in the 30-guard SOT pack. Strict typecheck, production build, API contracts, durable/coordination/timeout/producer/rate-authority backfill guards, lockdown guards, and the full SOT pack pass. The pre-existing imported-handler connection guard was repointed to recognize the already-documented isolated Rate Browser advisory-lock pool from `87a177de`; no runtime pool behavior changed. The only protected file touched was `src/routes/orders.ts`, for one read-only diagnostic field plus the required override comment; order lifecycle locks and all shipped/cancelled mutation protections are unchanged. No configured database/provider call, label/postage purchase, marketplace notification, inventory change, or production shipped/cancelled mutation occurred.

### Phase 5 — Strategic (quarter horizon)

- [x] 5.1 **Rate-on-ingest + exact signature cache complete** (2026-07-15, per user override `unlock shipped data`): after terminal-preserving upsert and package-fact materialization, `store-order-import.ts` admits only genuinely new `awaiting_shipment` source identities to the backend rate backfill owner. Audit 5.5 superseded the original process-local 100-order drip with durable pg-boss admission that preserves each request's exact target IDs, while `rates-backfill.ts` retains `cache_first` execution and delegates cache/live selection plus combined Best Rate authority unchanged; the scheduled PS-348 sweep remains restart/crash recovery. Official reuse stays on the existing `rate_cache.cache_key` exact fingerprint--exact ZIP/origin, weight, dimensions, options, residential state, ship-date/version policy, client/store/source credential/account identities--because a coarser zone/dims-class key could cross delivery-area surcharge truth. The PS-336 placement record is `docs/ps-tickets/audit-5.1-rate-on-ingest.md`; `test:audit-rate-on-ingest` behaviorally proves durable payload provenance/target preservation and exact cache misses and remains mandatory in the SOT pack. Strict typecheck, production build, full SOT pack, PS-050/079/094/277/313/348/401, targeted/durable/coordination/timeout backfill, store connector/import, API-contract, and lockdown proofs pass. The shipped-adjacent shared importer changed only to read its terminal-preserved result and admit awaiting rows; terminal CASE behavior, shipments, edit locks, label/postage, inventory, and marketplace-confirmation authority are unchanged. No migration, configured database/provider call, real label/postage purchase, marketplace notification, inventory change, or production shipped/cancelled mutation occurred.
- [ ] 5.2 Optional: local tariff engine with nightly live-calibration probes
- [x] 5.3 **PO Box eligibility axis in address-classification owner complete** (**R-9**, 2026-07-15, per user override `unlock shipped data`): `address-classification.ts` now derives a separate US PO Box fact from explicit `PO Box`/`Post Office Box` street-line forms without changing residential/commercial truth or misclassifying PMB, rural-route `Box`, bare `Box`, or `Boxwood` addresses. The backend `shipping-service-eligibility.ts` owner rejects UPS/FedEx for PO Box destinations while leaving USPS and non-PO-Box behavior unchanged; rate fingerprints carry `pb=1|0` so incompatible cached rows cannot cross the address axis. Order-backed browse, backfill, cached-result filtering, saved Best Rate checks, the Orders read model, and the final label-purchase boundary all delegate to those owners; no frontend business rule or migration was added. The PS-336 placement record is `docs/ps-tickets/audit-5.3-po-box-eligibility.md`; `test:audit-po-box-eligibility` proves classification variants/false positives, carrier policy, cache separation, caller wiring, and pre-provider label enforcement and is mandatory in the 32-guard SOT pack. Focused PS-050/057/079/094/127/128/186/202/245/261/276/277/302/303/313, API-contract, selected-rate-proof, direct-label, print-queue, backfill, editable-order lockdown, strict typecheck, production build, and full SOT proofs pass. Protected edits only add read-only effective-destination evidence and awaiting-only pre-persist/pre-purchase rejection; existing shipped/cancelled guards and shipment history are unchanged. No configured database/provider call, real label/postage purchase, marketplace notification, inventory change, migration, or production shipped/cancelled mutation occurred.
- [x] 5.4 **Orders and Inventory table virtualization complete** (**FE-6**, 2026-07-15, per user override `unlock shipped data`): TanStack Virtual now windows loaded Orders and Inventory rows only above the shared 40-entry threshold, preserving the exact legacy DOM path for short tables. `table-virtualization.ts` owns the UI-only threshold, overscan, and spacer math; the shared `Table.tsx` exposes opt-in virtualization while leaving expansion/footer modes unchanged, and Inventory opts in only for the normal Stock Levels table. The specialized Orders table virtualizes one stable presentation list of SKU group headers plus order rows, preserves sorting/grouping/selection/actions and the existing `isReadOnly` group gate, and scrolls keyboard navigation to rows that are not yet mounted. The PS-336 placement record is `docs/ps-tickets/audit-5.4-table-virtualization.md`; `test:audit-table-virtualization` proves threshold/spacer behavior, caller wiring, read-only/keyboard preservation, and bounded browser proof and is mandatory in the 33-guard SOT pack. The full Orders/Inventory browser suite passes 8/8, including bounded mounted-row counts, scroll-to-final-row behavior, five-row fixture counts, and Awaiting/Shipped byte-parity; strict typecheck, production build, Orders/Inventory focused guards, lockdown guards, and the full SOT pack pass. A separate local test commit re-pinned pre-existing July 14 Orders DOM snapshot drift before the virtualization proof. The only protected file touched was `OrdersView.tsx`, for a presentation-only scroll ref with the required override comment; `OrdersTable.tsx`, `OrderRow.tsx`, `OrdersResultsShell.tsx`, and `OrdersView.css` changed only shipped-adjacent rendering/windowing. Existing shipped/cancelled selection and mutation protections are unchanged. No backend business rule, configured database/provider call, real label/postage purchase, marketplace notification, inventory mutation, migration, or production shipped/cancelled mutation occurred.
- [x] 5.5 **Multi-instance readiness complete** (**R-5** and friends, 2026-07-15, per user override `unlock shipped data`): all explicit, targeted, and rate-on-ingest backfill admission now persists exact request payloads in the existing pg-boss rate worker queue; the cross-process sync-lane lock remains the sole execution admission owner, busy-lane deferral preserves target IDs, and a failed deferral retries the original durable job instead of dropping it. The reused one-connection producer owns transport lifecycle only. Process-local backfill maps are worker telemetry and completed entries prune to 25. Rate Browser reservation now waits on its dedicated PostgreSQL advisory lock, rechecks `rate_browse_jobs`, and fails closed without provider fan-out if durable admission is unavailable; the bounded same-process promise map remains only an optimization. The automated Best Rate persistence boundary now uses optimistic JSONB compare-and-swap with conflict re-read/re-evaluation, closing the read-then-upsert ratchet race across replicas while preserving the canonical pure no-downgrade rule. The PS-336 placement/state inventory is `docs/ps-tickets/audit-5.5-multi-instance-readiness.md`; `test:audit-multi-instance-readiness` runs the real concurrent ratchet against ephemeral PostgreSQL and pins every durable admission/fail-closed/pruning boundary, and is mandatory in the 34-guard SOT pack. Strict typecheck, production build, full SOT pack, PS-111/121/271/313/348/350/403, API contracts, backfill durability/coordination/timeout/DB-pool, sync admission/advisory-lock/deadlock, and focused Audit 5.1 proofs pass. Protected/shipped-adjacent files touched were `src/routes/orders.ts` and `src/services/store-order-import.ts`, only to await durable admission for work still filtered to `awaiting_shipment`; `assertOrderEditable`, terminal-preserving import CASE logic, shipped/cancelled selection gates, shipments, labels/postage, inventory, and marketplace confirmation were not weakened. No migration, configured database/provider call, real label/postage purchase, marketplace notification, inventory change, or production shipped/cancelled mutation occurred.
- [x] 5.6 **`orders.raw` bounded payload policy complete** (**M4**, 2026-07-15, per user override `unlock shipped data`): `order-raw-payload-policy.ts` now owns one-copy persistence and a bounded ShipStation operational/reconciliation projection at the shared store-import boundary. Full `shipTo`, dimensions, shipping options, lifecycle/repair evidence, and direct-marketplace provider shapes remain available; duplicated ShipStation `items`, `billTo`, raw weight, notes/noise, and all exact `raw_source_payload` copies no longer persist. PostgreSQL JSONB already supplies TOAST compression, and moving provider PII to another DB table would not relieve disk pressure, so no second archive was added. The dry-run-first existing-row compactor delegates to the same owner, requires explicit apply confirmation, caps apply scans at 1,000 rows, preserves status/timestamps/all non-raw columns, and uses optimistic raw-value predicates so concurrent sync wins. Read-only production measurement found 41,019 exact duplicate payloads (76 MB logical), 32 MB duplicated raw items, and roughly 48 MB in other removed ShipStation bulk; a 10,000-row dry-run slice estimated 23.2 MB removable without writes. The PS-336 placement/runbook is `docs/ps-tickets/audit-5.6-orders-raw-payload-policy.md`; `test:audit-orders-raw-payload-policy` is mandatory in the 35-guard SOT pack. Strict typecheck, production build, the full 35-guard SOT pack, connector/import/source-identity, marketplace/Shopify, billing, order-lockdown, and PS-245 proofs pass; the runbook records one unrelated pre-existing stale PS-205 static-regex failure whose behavior assertions pass and whose guarded owner is unchanged. The only protected/shipped-adjacent file changed is `src/services/store-order-import.ts`, solely to delegate retention before the existing terminal-preserving upsert; terminal CASE behavior, effective lifecycle locks, shipments, labels/postage, inventory, billing, and marketplace confirmations are unchanged. No migration, compactor apply, configured database write, provider call, label/postage purchase, marketplace notification, inventory change, or production shipped/cancelled mutation occurred.
- [x] 5.7 **Billing close workflow UX complete** (2026-07-15): the selected-client Billing detail panel now reads finalization and credit-note DTOs directly from the scoped Audit 3.6 endpoints, captures the exact client/range before confirmation, and renders backend-frozen subtotal, credited amount, balance, row counts, actor, timestamp, and immutable credit history without recomputing billing money in React. Open periods expose a confirmed **Finalize period** action; overlapping closes visibly lock edit, warning-review, no-box-cost, zero-shipping-review, and HUGRAB bulk entry points, while a failed/pending lock read disables them until the backend verdict is known. Credit memos collect amount/reason plus a stable per-payload retry key, then replace the display only with the backend response. `billing-finalization-policy.ts`, scoped write routes, PostgreSQL finalization/credit triggers, overlap serialization, idempotency, and balance limits remain the sole authority. The PS-336 placement record is `docs/ps-tickets/audit-5.7-billing-close-workflow-ux.md`; `test:audit-billing-close-workflow-ux` is mandatory in the source-of-truth pack, and the route-mocked browser proof covers open → finalize → locked edits → append credit → refreshed balance/history. Strict typecheck, production build, PS-412 finalized-billing, PS-416 fail-closed billing, focused Audit 5.7, browser, and full source-of-truth proofs pass. No protected locked file or backend policy/schema changed, and no configured database/provider call, real invoice finalization/credit, label/postage purchase, inventory change, marketplace notification, or production shipped/cancelled mutation occurred.

### Ops actions (no code)

- [x] O-1 **Supabase disk/autoscale headroom verified** (2026-07-15): the signed-in Compute and Disk view showed 1.79 GB used of the spend-capped 8 GB gp3 disk (22.4% used, 6.21 GB free), comprising 1.05 GB database, 0.56 GB WAL, and 0.18 GB system. This leaves about 5.41 GB before the 90% autoscale threshold; `default_transaction_read_only` was off and no replication-slot WAL leak was present. The spend cap remains enabled, no disk/billing/provider setting changed, and the separate expired billing-grace warning was surfaced for human review.
- [x] O-2 **No-dims awaiting order repaired through the guarded backend owner** (2026-07-15): a production read-only dry run found 1 of 23 eligible awaiting orders missing complete dimensions and no unresolved candidates. Order 1468108 (Shopify `#1001`, `TEST-SKU-001`) had no active label or partial operator dimensions; its product default, package catalog row 504, and two same-SKU sibling orders all agreed on 2x42x22. The existing row-locked order override command persisted package 504 plus 2x42x22 while the order was still effectively awaiting, then durable targeted rate job `8c1a268d-85e6-4661-af13-5138852958b9` completed 1 updated / 0 skipped / 0 failed. Final read-back showed `best_rate_dims=2x42x22`, a fresh backend-issued rate, zero shipments, and no label/postage purchase, marketplace notification, inventory mutation, or shipped/cancelled change.
- [x] O-3 **Supabase leaked-password protection enabled** (2026-07-15): DJ manually enabled and saved the HaveIBeenPwned password check in the signed-in production project dashboard, then confirmed completion in-session. The setting is provider-managed and is not exposed by the project's public `/auth/v1/settings` readback; no other authentication, database, or application setting was changed by Codex.
- [ ] O-4 PS-406-style one-time check that order 1490383's recovery reused the provider label (no double purchase on 2026-07-13)

---

## Related context

- **2026-07-13 read-only incident:** Supabase applied `default_transaction_read_only=on` during a disk event ~07:00–08:22 UTC; connections opened in the window stayed poisoned (no pool `max_lifetime`). Batch job e0daf3ad failed 8/10; order 1490383 recovered at 09:06 UTC. Hardening = items 1.9 + O-1.
- **Rates/print-queue handoff review (same day):** all file/line claims verified accurate; the original `scripts/probe-batched-rate-estimate.ts` was never committed, so it was rebuilt on 2026-07-14 with an offline CI contract and an operator-gated live path; "one ShipStation key" is three credential-source classes (primary env, KFG env, and active client keys), and every batch is probed within one distinct source. Handoff artifact: <https://claude.ai/code/artifact/9b800cf6-72a3-4f5e-ae95-fe1c4e75e52c>
